// tests/deno-globals.d.ts — S-BRIEF-2PASS
//
// Зачем: `_shared/llm.ts` — Deno-модуль, и до этого спринта его не проверял НИКТО.
// `tsconfig.exclude` держит `supabase/functions` вне корневого набора файлов, но
// `exclude` фильтрует только стартовый список — модуль, импортированный тестом,
// в граф проверки всё равно попадает. Это ХОРОШО (у брифа теперь два прохода, и
// ошибка типов там дороже, чем на фронте), но требует объявить глобаль Deno.
//
// Объявление минимальное — ровно то, что edge-код реально использует. Расширять
// по мере надобности; шире делать незачем, а `any` тут запрещён контрактом проекта.
declare const Deno: {
  env: {
    get(key: string): string | undefined;
  };
};
