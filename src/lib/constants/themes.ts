import type { Theme } from '@/lib/stores/theme-store';

/**
 * Свотч темы — кружок, которым тема представлена в списке выбора (шапка и
 * настройки). Значение — `--accent` соответствующего блока `globals.css`.
 *
 * ЕДИНСТВЕННЫЙ ИСТОЧНИК. До S-TOKENS-CONTRACT-1 цвет был описан дважды —
 * в `ContentHeader.THEME_SWATCHES` и в `SettingsContent.THEMES` — и три темы
 * из семи расходились: frost `#5b8aff` против `#6ba3be`, aurora `#a060ff`
 * против `#7c6bc4`, tidal `#48b890` против `#4a9e8e`. В настройках человек
 * выбирал тему по цвету, которым она интерфейс не красит.
 *
 * ⚠️ Значение обязано совпадать с `--accent` своего блока в `globals.css`
 * (исключение — aura, см. ниже). Читать переменную из CSS в рантайме нельзя:
 * свотч рисуется для НЕ активной темы, а `getComputedStyle` отдаёт только
 * активную.
 */
export const THEME_SWATCH: Record<Theme, string> = {
  /* Лайм — единственная тема, где свотч равен --accent буквально: цвет
     достаточно насыщен, чтобы читаться кружком (в отличие от графита ауры). */
  't-lime': '#C9F25A',
  /**
   * Осознанное исключение: у ауры `--accent: #484D57` — графит, акцент
   * намеренно НЕ цветной (см. theme-system.md). Серый кружок в списке тем
   * неразличим и бесполезен, поэтому берём янтарь атмосферы — это
   * `--aura-orb-2` секций `leads`/`projects` (`224, 160, 58`), тот самый
   * оттенок, которым аура реально светится. `--accent-text` тут не подходит:
   * он тоже графитовый (`#343840`).
   */
  't-aura': '#E0A03A',
  't-washi': '#C23B3B',
  't-fuji': '#2B5078',
  't-frost': '#5b8aff',
  't-aurora': '#a060ff',
  't-tidal': '#48b890',
  't-minimal': '#0E7C86',
};
