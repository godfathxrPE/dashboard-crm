/**
 * Порог тишины: контакт считается «остывающим», если последнее состоявшееся
 * касание (звонок/встреча) старше этого числа дней.
 *
 * R2-P0-D: значение стало настройкой организации (`organizations.settings.reconnect_days`,
 * миграция 076). Эта константа — дефолт и фолбэк, пока настройка не задана / не загружена.
 * В клиентских компонентах читать через `useReconnectDays()` (hooks/use-org-settings),
 * в чистых функциях — принимать порог параметром.
 */
export const DEFAULT_RECONNECT_DAYS = 21;

/**
 * @deprecated Алиас на один спринт, чтобы диф не разъехался разом по всем потребителям.
 * Новый код: `useReconnectDays()` (клиент) или явный параметр (чистые функции).
 */
export const RECONNECT_THRESHOLD_DAYS = DEFAULT_RECONNECT_DAYS;
