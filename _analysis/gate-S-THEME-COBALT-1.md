# Гейт S-THEME-COBALT-1 — PR #119 (`02eb470`)

**24.09.2026.** Работа `6941367` от `ec656c4`, squash → `02eb470`. Спринт —
`_analysis/sprint-S-THEME-COBALT-1.md`. Миграций нет.

## Итог

Принято. Дефолтная тема — кобальт.

## Что проверено

- Дифф (11 файлов, +111/−139): токены `.t-cobalt` = таблица спринта; `--zone-work` 8% после
  `:root`; три костыля лайма удалены; правила нава, рельса, `.bg-green`, `.bg-yellow`
  переименованы. Комментарий `DealHeader` точен: буква плитки — `--bg`, не акцент.
- Миграция выбора: FOUC-гард и `resolvePersistedTheme` не знают `t-lime` → дефолт. Тест
  «сохранённый t-lime → новый дефолт»; вспышки нет (проверка исполнителя).
- `audit-contrast.py`: ветка лайма снята, затемнение `bg-green` сохранено; `t-cobalt` 0 FAIL / 39,
  слабейшая пара — белый на циане 4.70. Всего 3 FAIL (nav-active тёмных тем), как до спринта.
- ΔE зон при 8%: «Работа ↔ Контекст» 5.7 (порог 5), к «Рискам» 9.3–13.6 — посчитано гейтом.
- Линзы: базовая ✓ · design ✓ (токены, контраст скриптом) · a11y ✓ (ring `--accent-text` 7.03) ·
  security/api/supabase — n/a.

## Находки

- 🟡 Мелкий текст акцентом (`РАБОТА`, `ЗАКРЫТИЕ`, «Развернуть», коннекторы) стал заметнее:
  насыщенный `#1D4BC0` против оливкового `#336809`. Бюджет пятен Р2 не нарушен; если шумит —
  `--zone-work-ink` → `--text-dim` одной строкой. Решение владельца по живому экрану.
- 🟢 Свотчи Cobalt и Frost близки по тону (светлая и тёмная темы).
- 🟢 Циан глазами не проверен — запланированных звонков нет; контраст подтверждён аудитом.
- 🟢 У исполнителя по клику открылась модалка перехода стадии — закрыта «Отмена», ничего не записано.

## Verification

```
Type Safety:            PASS (tsc чистый у исполнителя, Record<Theme,…> держит свотч)
RLS Coverage:           NOT_APPLICABLE
Backward Compatibility: PASS (сохранённый t-lime → дефолт, тест)
Runtime Tested:         PASS у исполнителя (dev); прод — после деплоя
Regional Availability:  NOT_APPLICABLE
```
