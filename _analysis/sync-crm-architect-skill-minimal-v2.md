# Sync crm-architect skill — Minimal v2 → v2.4

**Дата:** 2026-08-21 · **Повод:** линия Minimal v2 влита в `main` (`eb8b99e`).
Скилл описывает Minimal до правок и молчит о трёх вещах, на которых уже споткнулись:
примитив `.sheet`, неработавшие альфа-модификаторы и запрет модификатора на `-l`-токенах.
Будущая сессия, читая скилл, напишет `bg-accent-l/20` снова.

Источник скилла — `~/.claude/skills/crm-architect/` (в репо не вендорится).
Правки чисто документационные, кода не касаются. Три файла: `SKILL.md`,
`references/theme-system.md`, `references/learnings.md`.

Контекст целиком — `_analysis/handoff-minimal-v2-2026-08-21.md`.

---

## 1. `references/theme-system.md` — строка `t-minimal` в таблице тем

Устарело двумя фактами: canvas сменился в v2, радиусы никогда не были «острыми» после
S-TOKENS-GEOM.

**old** (фрагменты внутри строки таблицы):
```
Нейтральный canvas `#F6F6F7` / `#FFFFFF` (Linear/Attio class).
```
**new:**
```
Нейтральный canvas `#ECECEF` / `#FFFFFF` (Linear/Attio class; v2 затемнил фон с `#F6F6F7` — белые карточки отделяются без тяжёлых теней).
```

**old:**
```
**Острые углы** (`--radius-l: 8px`, острее прочих ~12–14).
```
**new:**
```
Радиусы средние (`--radius: 10px`, `--radius-m: 10px`, `--radius-l: 14px`) — «острая» тема здесь washi (`--radius: 4px`).
```

---

## 2. `references/theme-system.md` — раздел «Радиусы»

**old:**
```
`--radius-s/-m/-l` задаются per-theme (`t-minimal` острее: `--radius-l: 8px` против ~12–14 у прочих).
```
**new:**
```
`--radius-s/-m/-l` задаются per-theme. Разброс `--radius-l`: washi 8, tidal/aurora 12, fuji/minimal 14, aura 18, frost 16. Острая тема — washi (`--radius: 4px`), не minimal.
```

---

## 3. `references/theme-system.md` — раздел «Канон теней (elevation vs card)»

Дописать четвёртым буллетом, после «Грабля: `hover:elevation-N` не работает»:

```
- **Лист** → `.sheet` (`@layer components`): `--surface` + `--border` + `--radius-m` +
  `--shadow-card`. Единственный примитив подложки; `ui/Card.tsx` удалён (v2.3) как мёртвый.
  Не писать `bg-surface border border-border rounded-xl` руками — это ровно то, что чинили
  в v2.1–v2.2 (≈300 мест мимо любых токенов тени).
  **В стеклянных темах** (`t-frost`/`t-aurora`/`t-tidal`) `.sheet` обязан стоять в блоке
  Dark-theme glassmorphism рядом с `.shadow-card`: там `--surface` полупрозрачен и лист
  держится на `backdrop-filter`, иначе читается грязным пятном.
  Иерархия: `.sheet` — статичная подложка, `.elevation-hover` — карточка под курсором,
  `.shadow-card` — карточка доски.
```

---

## 4. `references/theme-system.md` — новый раздел про альфу

Вставить после раздела «Палитра vs семантика», до «Заливка vs текст»:

```
**Альфа-модификаторы Tailwind.** Цвета в `tailwind.config.ts` отдаются функцией-значением
с `opacityValue` (v2.3): без модификатора — чистый `var(--x)`, с модификатором —
`color-mix(in srgb, var(--x) calc(N * 100%), transparent)`. До этой правки цвета были
объявлены строкой `'var(--x)'`, и Tailwind **не генерировал класс вовсе**: 309 мест в коде
не давали ни фона, ни цвета рамки (блоки ошибок читались как обычный текст, подсветка
drop-зон не появлялась).

Два следствия, оба обязательны к соблюдению:

1. **`-l`-токен + модификатор — запрещённый паттерн.** `--accent-l`, `--red-l` и прочие уже
   полупрозрачны (8–15%); модификатор даёт процент от процента — `bg-accent-l/20` в Minimal
   это альфа **0.0157**, не видно ни в одной теме. Активное состояние (drag-over, выбранная
   опция, hover поверх выбранного) → **`--accent-l2`** (16–25%, объявлен во всех темах);
   спокойная подложка → `-l` без модификатора; нужна точная альфа → обычный токен
   (`bg-accent/10`).
2. **Функция обязана отдавать чистый `var()` не только при `opacityValue === undefined`.**
   Tailwind зовёт её и для базовой утилиты, подставляя `var(--tw-*-opacity)`; без проверки
   `opacityValue.startsWith('var(')` в `color-mix` уедут **все** цветовые утилиты, включая
   `.border-border`, на котором держится плотность бордеров v2.
```

---

## 5. `references/learnings.md` — раздел про наложение тинтов

Дописать абзацем к существующему тексту «Тот же приём стоит помнить при любом наложении
тинтов…»:

```
С 2026-08-21 наложение `-l`-тинтов ещё и запрещено технически: модификатор альфы на
`-l`-токене даёт 1.5–5% и не читается ни в одной теме (v2.4, 29 мест). Для активных
состояний есть `--accent-l2`.
```

---

## 6. `references/learnings.md` — новый абзац в процессные грабли

```
**`npm run build` затирает `.next` под работающим `next dev`.** Процесс остаётся живым, но
приложение отдаёт 404 на CSS-чанки и рендерится голым HTML — выглядит как «сломалась тема».
Гасить dev перед билдом либо перезапускать после. Смежное: `xargs -r` — GNU-ключ, на macOS
команда гашения молча не срабатывает. И отдельно: если правило есть в CSS-файле, но
отсутствует в computed, — это вкладка держит стайлшит, загруженный до пересборки, а не дефект.
```

---

## 7. `SKILL.md` — строка Project Identity

Если там перечислены UI-примитивы или паттерн карточки — привести к `.sheet`.
Если такой строки нет, добавить в блок Conventions:

```
| Лист / карточка | `.sheet` (подложка) · `.elevation-hover` (под курсором) · `.shadow-card` (карточка доски). `ui/Card.tsx` удалён 2026-08-21 |
```

---

## Проверка после правок

```bash
grep -n "F6F6F7" ~/.claude/skills/crm-architect/references/theme-system.md   # → пусто
grep -n "radius-l: 8px" ~/.claude/skills/crm-architect/references/*.md        # → только про washi
grep -rn "\.sheet" ~/.claude/skills/crm-architect/references/theme-system.md  # → есть
grep -rn "opacityValue\|accent-l2" ~/.claude/skills/crm-architect/references/ # → есть
```

Если скилл вендорится ещё и в `dashboard-crm/crm-architect/` — синхронизировать обе копии,
иначе следующая сессия прочитает ту, что осталась старой.
