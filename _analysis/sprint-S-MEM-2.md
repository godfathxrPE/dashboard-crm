# S-MEM-2 — содержательный разбор памяти crm-architect

**2026-08-22 · только `crm-architect/references/` + журнал CC (через S-MEM-2a утром)**
Родитель: S-MEM-1 (механическое расщепление). Этот — содержательный: из хроники в тематику.

## Сделано

1. **learnings.md 48 → 68,5 КБ, структура 18 тематических разделов.**
   Из `journal.md` (22 раздела, 123 КБ) поднято ~45 переиспользуемых правил — каждое
   строкой-двумя со ссылкой на раздел журнала, по правилу двух файлов. Пять новых разделов:
   «Edge-функции и шлюз», «Типы и реген», «Домен: факты, дающие ноль строк без ошибки»,
   «LLM-структурированный вывод», «Интеграции с внешним состоянием», «Тесты и смоки».
2. **Обе «Волны 2» растворены**: 11 записей перенесены дословно в тематические разделы
   (Radix→React, TZ→Даты, derived→Типы, RLS-зеркало/canManage→Multi-tenancy,
   мерж-после-apply→Процесс и т.д.). Заголовки волн удалены — справочник снова тематический
   насквозь.
3. **Хвост 128** (два правила после «---») встроен в Supabase-раздел.
4. **architecture.md измерен**: хроники НЕТ — 4% строк со спринт-маркерами, и это привязки
   паттернов («(Sprint W2c)»), не хроникальные разделы. Опасение хендоффа снято фактом,
   резать нечего.
5. **SKILL.md измерен по разделам** (17K знаков: Conventions 3.6K, Reference Files 3K,
   Гейт 2.9K, Checklist 2.2K…): всё рабочее, подрезка дала бы ~2 КБ ценой риска протокола.
   Решение — НЕ резать. (Утром S-MEM-2a: MEMORY.md журнала CC 39→13,7 КБ, архив побайтово.)

## Метод

Переносы — только exact-match replace с assert (ни один существующий текст не перенабирался
руками); новые правила — дистилляция полного прочтения журнала со сверкой на дубли против
полного прочтения learnings. Рост файла +20 КБ — это новые правила, не хроника.

## Замечание владельцу проекта (вне скоупа, требует решения)

Контракт проекта (system prompt CRM Architect и SKILL) до сих пор декларирует
«Soft delete MUST be the default», а в схеме нет `deleted_at` ни у одной таблицы — проект
на hard delete (вскрыто S-FIX-BATCH-1, зафиксировано теперь и в learnings «Процесс»).
Либо миграция с `deleted_at`, либо правка контракта. Пока не решено — спеки продолжают
ставить невыполнимые требования.

## Verification

```
Целостность:  PASS — все 11 записей волн перенесены (grep «Волна» = 1 историческая ссылка
              внутри текста записи); структурные заголовки 18 разделов; переносы assert'ами
Полнота:      PASS — journal.md прочитан целиком (22/22 раздела), learnings целиком
Runtime:      NOT_APPLICABLE (документы)
```
