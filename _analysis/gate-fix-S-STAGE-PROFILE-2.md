# Гейт fix-S-STAGE-PROFILE-2 — PR #123 (`db94699`)

**26.09.2026.** Работа: `22a82f1`, squash → `db94699`. Прод-деплой
`dpl_14Y5NchwracYGGYATZpWwVaD5rWE` (READY), откат — `dpl_79Em9RtNVpzrcinH3ew5Tc3Dw9tP`.
Фикс-файл — `_analysis/fix-S-STAGE-PROFILE-2.md` (лёг в `main` #122 до запуска). Миграций и
новых запросов нет.

## Что сделано

- **P-4.** `useOrgSettingsReady()` = `!isPending` у `useOrgSettings()`; `ProjectStageCockpit`
  до ответа настроек рисует заглушку «Загружаем нормы стадий…» высотой строки (116 px) вместо
  строки и карты. Лид (`LegacyRow`) не тронут.
- **P-5.** `buildStageProfile`: масштаб — `max(нормы, факт текущей)`; без текущей (won) и без
  норм — по всем фактам, как раньше. Не-текущая колонка выше столбика обрезается до
  `PROFILE_BAR_H` с `clipped: true`; в `StageProfile` — щель 2px `bg-surface` на 6px ниже верха,
  `title`/`aria-label` «… при норме N — столбик обрезан».
- **washi.** `--profile-now` (`:root` = `--mark-today`, `.t-washi` = `--text` 78%) — только
  заливка текущей; контур, узел, «сегодня», шкала кокпита — на `--mark-today`.
- Тесты: 19 → 23 в `stage-profile.test.ts`; правка одного старого кейса («pxPerDay < 1» —
  его масштаб задавал именно выброс прошлой), покрытие то же.

## Отступление исполнителя — принято

Обрезается и история будущей колонки (откат), не только прошлая: новый масштаб её тоже не
вмещает по построению.

## Проверка

**Дифф прочитан целиком.** `useOrgSettings` без `enabled: false` — `isPending` гарантированно
разрешается, вечной заглушки нет; при ошибке `retry: 1` (`QueryProvider.tsx:22`) — ~1 с сверху,
затем дефолты. Ранний `return` в `ProjectStageCockpit` стоит после всех хуков компонента
(единственный хук ниже, `useState:416`, — в `StageGuidance`). Щель `absolute` внутри заливки
с `relative`.

**Прод после мержа**, сделка «ЭЙЧ ЭНД ЭН» (`aa2c3628…`):

| Проверка | Итог |
|---|---|
| Масштаб | ✅ «Лид» 62/2 и текущая «Квалификация» 11/3 — обе 89.5 px; раньше текущая ~13 px |
| Обрезка | ✅ `title` «Лид · 62 дн. при норме 2 — столбик обрезан», щель 1.99 px `rgb(255,255,255)` = `--surface` |
| washi (класс в DOM) | ✅ низ текущей `rgba(44,44,44,.78)`, прошлый факт — 55%: текущая темнее прошлого, не плита. Чистый `ok` на живых данных не увидеть — все открытые сделки просрочены |
| P-4, живая загрузка без троттлинга | ✅ перезагрузка страницы: заглушка появилась на 6.0 с от старта навигации, сменилась картой на 8.1 с; ячеек шкалы под заглушкой — 0 |

Живой замер P-4 заодно подтвердил I-1: настройки организации на проде приходят через 6–8 с.

## Находки

- 🟡 **P-6 — P-4 закрыт только в кокпите.** До загрузки настроек дефолтные пороги групп
  по-прежнему рисуют `DealSignals` («В стадии N дн. при норме M»), `ProjectsTable` (бейдж
  «залипла»), `useStageTimeGauge` (`ProjectCard`, `ProjectPeekContent`), `useStageNormDateKey`
  (пунктир нормы в `DealDeadlineTrack`). Лечение — тот же `useOrgSettingsReady()` у каждого
  потребителя, ~1 ч. Список — из разведки исполнителя.

## Verification

```
Type Safety:            PASS (tsc чистый у исполнителя и в CI; дифф прочитан)
RLS Coverage:           NOT_APPLICABLE (запросы и схема не менялись)
Backward Compatibility: PASS (лид не тронут; allDone/без норм — прежний масштаб, тесты)
Runtime Tested:         PASS на проде (масштаб, обрезка, washi, заглушка на живой загрузке)
Regional Availability:  NOT_APPLICABLE
```
