# Handoff — сессия 2026-07-07/08

Итог рабочей сессии по dashboard-crm. Что сделано, что осталось, куда дальше.

---

## Сделано (в хронологии)

### 1. AI Hub — концепт и планирование
- **`AI-HUB-CONCEPT.md`** — концепт AI-блока CRM (транскрипт → протокол/аналитическая
  записка/SPIN-разбор). Ключевые решения: пресеты вместо чата, промпты в коде (не в БД),
  транскрипт как отдельная сущность, «AI предлагает — юзер подтверждает», учёт токенов
  с первого дня, кэш-роллапы вместо живой генерации.
- **`_analysis/sprint-ai-1-ai-hub.md`** — sprint prompt S-AI-1 (после 2 раундов ревью:
  атомарный CAS-реклейм зомби-прогонов, `{claim,quote}` против галлюцинаций, `feedback_note`).

### 2. Гейт-хвосты S28 закрыты (injection + HTTP-смоки)
- **`_analysis/injection-test-s28.md`** — 6 сценариев injection-теста, прогнаны через UI:
  6/6 PASS (включая false-positive-проверку). Вердикт: anti-injection контур S28
  валидирован как baseline для S-AI-1.
- **`_analysis/smoke-s28-negative.sh`** — негативные HTTP-смоки: 6/6 PASS (401/400/405).
- **`BACKLOG.md`** — три блокера переведены в done (кредиты Anthropic, гейт-хвосты S28/S29).

### 3. S-AI-1 (AI Hub MVP) — задеплоен и смокнут
- **Миграция 030** (`transcripts` + `ai_runs`) применена на прод (в history).
- **Edge `ai-run`** задеплоена (3 пресета: протокол / аналитическая записка / SPIN;
  async через waitUntil, anti-injection, forced tool_choice, JWT+RLS). Модель Sonnet
  через env, дефолт `claude-sonnet-5`.
- Advisors чистые (initplan-обёртки на новых policies есть). Смок пройден на живом звонке —
  аналитическая записка с цитатами-основаниями сгенерировалась корректно.

### 4. Фикс скролла модалок
- `CallModal`/`MeetingModal` — `max-h-[85vh] + overflow-y-auto` (AI-контент выходил за вьюпорт).

### 5. Contact Hub (Sprint Contact-Hub-A) — переиспользуемый timeline
- **`_analysis/sprint-contact-hub-A.md`** — sprint prompt.
- Реализовано: `<EntityTimeline entityType entityId>` + `useEntityTimeline` (переиспользуемы
  для contact/company/project), серверный фильтр вместо org-fetch, адаптеры источников,
  группировка Просрочено/Этот месяц/Ранее, фильтр-табы. Миграция 031 (FK-индексы).
- Проверено браузером: серверный фильтр, табы без рефетча, reuse-смок на сделке — всё ✅.

### 6. Company/Deal Hub — timeline на всех сущностях
- **`_analysis/sprint-company-deal-hub.md`** — sprint prompt.
- `<EntityTimeline>` встроен в CompanyDetail (net-new) и ProjectDetail (заменил org-fetch
  списки + ActivityTimeline → composer заметок). Deal: `includeSystem` включён (activity_log
  + ai_runs в ленте). Общий `openTimelineEvent`. Нетто −149 строк. Регресс stage board чист.

### 7. Полиш модалки события (3 правки)
- **`_analysis/sprint-event-modal-polish.md`** — Combobox (поиск по подстроке, «Ромашка»
  вместо `ООО "Ро…`) + автоподстановка компании из контакта.
- **`_analysis/sprint-event-modal-autofill-open.md`** — автоподстановка при ОТКРЫТИИ
  модалки с карточки контакта (не только ручной выбор) + автоподстановка проекта.
  Хелпер `deriveFromContact`. Гейты: edit-режим исключён, только пустые поля.

### 8. Обсуждения без кода (зафиксированы решения)
- **Company timeline и many-to-many:** активность привязывается к компании явно через
  `company_id`, НЕ размазывается по всем компаниям контакта (иначе разговор про компанию A
  всплывёт в ленте B). Поведение оставлено как есть — оно корректно.

---

## Оценки (написаны, не реализованы)

- **`_analysis/estimate-contact-hub.md`** — оценка Contact Hub + Contact AI (спринты A/B).
  A сделан (см. п.5). B — не сделан (см. ниже).

---

## Осталось — задачи

### Приоритет 1 — развилка «что дальше» (не решено)
Три кандидата, выбор за Олегом:
- **Cmd+K (S31, полиш)** — маленький, приятный, передышка после серии содержательных
  спринтов. `<EntityTimeline>` готов, хорошая точка для паузы.
- **Спринт B — Contact/Deal AI-роллапы** (`estimate-contact-hub.md`, §B) — ~0.5 спринта
  поверх S-AI-1. Пресеты `contact_digest`/`contact_health`, sentiment-гейдж, кэш-роллапы
  (`entity_type += 'contact'`). ⚠️ ПЕРЕД стартом: посмотреть по `feedback_note`, что пресеты
  S-AI-1 реально в ходу — иначе B будет дорогим демо-слоем.
- **S30 Telegram** — последний функциональный ноль скоркарты, самый тяжёлый (бот, привязка
  чатов к контактам/сделкам, входящие в timeline — `<EntityTimeline>` готов принять новый `kind`).

### Приоритет 2 — хвосты из BACKLOG.md (спокойные)
- **Real e2e инвайта** вторым аккаунтом (S26) — полный путь signup→membership живым юзером
  не проверен. Заодно закроет SKIP-кейс `user+nonexistent→404` в `smoke-s28-negative.sh`
  (нужен `USER_JWT`).
- **Legacy-читатели `projects.stage`** (S30+) — переключить на `stage_id`: ProjectsTable,
  ContactDetailHub, CommandPalette, ProjectCard/ProjectPeek, ProjectDetail.
- **Полный вынос legacy `stage`** + 3-track хардкода (после переключения читателей) —
  DROP колонки `projects.stage`, enum `deal_stage`, `STAGE_CONFIG`.
- **Meetings: `description` не в AI-контексте** (S28, минор — функция берёт только notes).

### Приоритет 3 — мелочи, накопленные в этой сессии
- **Тестовый мусор в звонках** — инъекционные звонки под «TEST» + тестовый звонок
  «АО ДРУЖБА / Наиля Алтынбаева» от 8 июл. Всплывали трижды не там, где ждёшь; путают
  ленты и AI-прогоны. Почистить.
- **`feedback_note` при 👎** — единственный непроверенный кусок S-AI-1 (30 сек в UI).
- **`any` в `CallModal:16`** (DetailsSection, `register: any`) — предсуществующий, не срочно.
- **Автоподстановка «компания → фильтр контактов»** — осознанно НЕ делали (many-to-many
  риск). Если попросят — отдельная задача.
- **Rollup непомеченной активности на компанию** (звонок без `company_id` → всплыть на
  компании контакта) — обсудили, признали неsrочным; на данных Олега пока не проявляется.

---

## Состояние проекта (скоркарта)
Фаза 1 закрыта. Фаза 2: гейты (S27), AI-инфраструктура (S28), автоматизация (S29+S29.1),
**AI Hub (S-AI-1) — done**, **Contact/Company/Deal Hub — done**. Из пяти нулей скоркарты
закрыты автоматизация, коллаборация, AI. Остаётся: коммуникации (S30 Telegram) и полиш
(Cmd+K, онбординг, дашборд руководителя).

## Технические заметки для следующей сессии
- Модалки лежат в фиче-папках (`calls/`, `meetings/`, `tasks/`), НЕ в `components/modals/`.
- Timeline-источники — серверный фильтр по entity-колонке, не org-fetch + useMemo.
- `_analysis/` под git (закоммичен), но новые файлы этой сессии могли остаться untracked —
  проверить `git status`, при необходимости `git add _analysis/`.
- Все sprint-промпты сессии лежат в `_analysis/sprint-*.md`.
