# Гейт S-DEAL-CHZ-2 — PR #134 (`52c0de9`)

**26.09.2026.** Работа `714874e`, squash → `52c0de9`. Прод-деплой `dpl_9xESAorFgr2t255qvKZNLyeEJByv`,
откат — `dpl_5dpCeBWKqvbCUH9z7VvnAZsRwuzK` (`cb9884e`). Спринт-файл переписан по сверке входа 26.09
(#133). Миграций нет. Последний спринт очереди эпика «Сделка v2».

## Что сделано

- `lib/data/chz-groups.ts` + зеркало `ai-run/chz-groups.ts`: `ChzPhase`
  (`mandatory | starting | planned | experiment`), `CHZ_START_HORIZON_MONTHS = 6`, `chzPhase(g, now)`,
  `phaseChzGroups(groups, now)` (стартующие первыми, внутри фазы — входной порядок),
  `chzStatusLabel(g, now)` — `now` обязателен. `matchChzGroups`/`STATUS_RANK` не тронуты.
- Все потребители на фазе: `DealChzCard`, `CompanySidebar`, `CompanyHighlights` (полоса берёт первую
  по фазе, `hot` по фазе), `CompanyBriefRenderer`, промпт `ai-run`.
- `ChzBadge`: проп — фаза; `starting` на семантическом `warning` вместо `yellow`.
- `DealChzCard`: группы — плашки `rounded-lg`; стартующая — `color-mix` от `--bg` (8 % фон,
  25 % рамка), остальные — `surface2`. Подвал «ОКВЭД {код} ↔ справочник 2026-08», полная дата и
  источники — в `title`.
- Тесты: 10 кейсов плана поведением + страж зеркал на фазу и подпись при трёх датах.

## Почему файл был переписан (сверка входа)

Статичный `status: 'starting'` снапшота 03.08 на 26.09 врал: у всех шести «стартующих» групп
`since` уже прошёл. Бейдж «стартует 2026-03» стоял на четырёх экранах и уходил в промпт `ai-run`;
по БД под такую группу попадали 8 из 26 сделок с компанией. Редакция от 06.09 сделала бы ложный
«горячий лид» громче. Сегодня по фазе подсвечена одна группа — «Мука, макароны, мёд» (2026-09).

## Отступления — приняты

1. `rounded-lg` вместо `rounded-xl` из плана — совпадает с `RailCard` и `DealStakeholders`; план
   написал радиус, не сверив.
2. Контрольный grep `status === 'starting'` находит `ChzBadge` — это проверка пропа-фазы, как задумано.

## Проверка

- Дифф `main..feat/deal-chz-2` — один коммит, 9 файлов, CSS не менялся (8 FAIL `audit-contrast` —
  состояние тем `main`, спринтом не задеты).
- Зеркало совпадает с `export type ChzStatus` до конца файла; импортов в `chz-groups.ts` — 0.
- Вызовов `chzStatusLabel` без `now` — 0.
- CI `checks` зелёный, превью Vercel READY; мерж — `gh pr checks --watch` → `merge --squash`.
- Визуальная приёмка исполнителя: «Мукомол» (ОКВЭД 10.61) в 8 темах, minimal первой;
  «Хороший вкус» — мясная группа «обязательна с 2026-08» на `surface2`; полоса компании подсвечена.
- Линзы: базовая ✓ · дизайн ✓ (только токены, `color-mix` от `--bg`) · a11y ✓ (смысл — текстом) ·
  security n/a · api n/a (меняется только текст промпта) · supabase n/a. Cold review не требовался.

## Находки

- 🟡 **P-8.** В тёмных темах плашка стартующей группы ТЕМНЕЕ карточки (`--bg` темнее `--surface`),
  а действующая на `surface2` — светлее: подсвеченная строка читается провалом. Дефект плана, не
  исполнителя: база `--bg` выбрана из-за glass-тем (learnings: `color-mix` от `--surface` там даёт
  прозрачность), тёмные темы до спринта не сверены. Ждёт взгляда владельца; в STATUS хвостом.
- 🟢 `new Date()` на SSR (Vercel, UTC) и в Deno против МСК у клиента: первые 3 часа месяца фаза
  может разойтись — одно предупреждение гидрации. Принято.
- 🟢 AI-бриф выводит группы в табличном порядке, а не по фазе. Косметика.

## После мержа — владелец

- `npx supabase functions deploy ai-run --project-ref uoiavcabxgdjugzryrmj` — до деплоя промпт
  AI-брифа продолжает получать статичную подпись.
- Пересверка снапшота справочника с честныйзнак.рф — вне скоупа.
