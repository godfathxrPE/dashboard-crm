# Спринт S-COCKPIT-ROW-1 — рестайл строки кокпита (W1, часть A)

**Вход:** `main` после мержа S-STAGE-PROFILE-1. **Ветка:** `feat/cockpit-row-1`.
**Миграций НЕТ. Новых запросов НЕТ.**

**СПЕКА ВЛАДЕЛЬЦА — `_analysis/spec-stage-profile.html`, раздел A** (A1 таблица правок,
A2 источники данных, A3 состояния). Она в репозитории; геометрия и токены оттуда,
здесь не дублируются.

⚠️ **Берётся ПОСЛЕ карты, не раньше.** Карта (S-STAGE-PROFILE-1) аддитивна: новый
компонент вместо соседнего блока, общий код не трогает. Строка кокпита — это
`PipelineCockpit`, компонент **общий с `LeadDetail`, `ProjectCard` и `DealStageStory`**.
Смешивать эти риски в одном спринте незачем, и спека сама разводит A и B.

---

## Что в спеке уже верно и берётся как есть

A1 целиком: чип «✓ N» уходит с лаймового фона на `surface2` (лайм резервируется за
действием и «идёт»), ячейка текущей стадии получает шкалу вместо бледной заливки,
готовность — точки 8px с `inset` (лайм на белом даёт 1.29:1 и не читается без обводки),
CTA становится сплошным `accent` с `--shadow-accent` (токен в проекте есть, проверено),
вероятность из кнопки уходит — она уже в `metaRight` и в карте.

A2 сверен с кодом и верен: `phaseLabel` из `lib/constants/phase-labels.ts`, `stages`
активные с сортировкой по `order_index`, `gauge` — тот же объект, что уходит в
`StageProfile.gauge`, гейт через `useStageRequirements` + `check_stage_requirements`,
`probability` у стадии существует (`number | null`).

A3 — состояния `ok`/`warn`/`over`/`locked`/`next.locked`/узкие ширины: по спеке.

---

## Поправки гейта

### П1 — `t-washi` и здесь

`BORDER_BY_STATE` уже существует в `PipelineCockpit` и красит рамку ячейки
`accent`/`yellow`/`red`. В `t-washi` `--accent` и `--red` — один `#C23B3B`
(`globals.css:320`/`:322`), то есть **рамка «идём по норме» и рамка «просрочено»
в этой теме одинаковы уже сегодня**, до всякого рестайла. Спринт карты вводит
`--mark-today` для состояния `ok`; здесь — то же самое, тем же токеном, чтобы ячейка
и столбик не разошлись.

Это существующий дефект, а не привнесённый: зафиксировать в отчёте как найденный,
а не как созданный.

### П2 — мини-карта: `ResizeObserver` против container query

Спека допускает оба варианта («скрывается при ширине строки < 720 — ResizeObserver или
`@container (max-width: 719px)`»). Брать **container query**: он не требует ни состояния
в React, ни подписки, ни очистки, и не даёт лишнего рендера на каждый ресайз. Проверить
поддержку в целевых браузерах и, если нужен фолбэк, тогда `ResizeObserver` — но не
раньше. Решение записать в отчёт с причиной.

### П3 — `localStorage` для состояния карты

Спека просит запоминать ручной выбор в `localStorage['cockpit-map:'+pipelineId]`.
Обязательно обернуть чтение и запись в `try/catch`: приватное окно и заблокированные
site data кидают на самом обращении, а не возвращают `null`. Значение по умолчанию —
`stages[currentIndex].phase_group === 'closing'`, как в спеке; при недоступном хранилище
поведение обязано остаться этим дефолтом, а не «свёрнуто всегда».

### П4 — лид не должен измениться

`gauge === null` (лид) — ячейка как сегодня, одна строка, без шкалы, мини-карта не
передаётся. **Это главный регрессионный риск спринта.** В приёмку: открыть карточку
лида и сверить, что строка кокпита выглядит ровно как до спринта; в отчёт — скриншот
до/после.

---

## ТЕСТЫ

Чистых функций спринт не добавляет — правится разметка и состояния. **Явная строка
«тестов нет: только разметка и состояния»** обязательна в отчёте (правило чек-листа
`crm-architect`). Если появится хелпер группировки мини-карты в `src/lib/` — он идёт
с тестом.

## ФИНАЛЬНАЯ ПРОВЕРКА

```bash
npx tsc --noEmit && npm run lint 2>&1 | tail -5 && npx vitest run 2>&1 | tail -5
python3 scripts/audit-contrast.py 2>&1 | tail -5
python3 scripts/audit-tokens.py 2>&1 | tail -3
npm run build 2>&1 | tail -5
```

Приёмка — чек-лист спеки (B, п.7, первые два пункта про кокпит и мини-карту) плюс:
`t-washi` отдельно, карточка лида без изменений, ширины 900 и 640 и 556.

## КОММИТ

```
feat(pipeline): строка кокпита по макету — шкала вместо заливки, мини-карта воронки

- ячейка текущей стадии: шкала h8 с маркерами «сегодня» и «норма», подписи дат
- чип пройденных уходит с лаймового фона: лайм зарезервирован за действием
- CTA сплошной accent + --shadow-accent, вероятность убрана (дубль metaRight)
- мини-карта групп справа, скрывается container query при < 720
- ok красится --mark-today: в t-washi accent === red — дефект существовал до спринта
```

**Не мержить.** Отчёт — на гейт.
