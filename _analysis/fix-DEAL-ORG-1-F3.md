# Fix DEAL-ORG-1-F3 — карточка КП печатает дату отправки под словом «Отклонено»

**Ветка:** `feat/deal-org-1` (worktree `.claude/worktrees/feat+deal-org-1`), поверх `890849d`.
**Источник:** гейт + cold review `_analysis/cold-review-feat-deal-org-1.md`, находка F3.
**Миграций нет. Правка одна.**

## Отказ

Сделка с единственным КП в статусе `sent`, `sent_at = 2026-09-01`. 7 сентября в карточке
жмут «Отклонено», указывают причину. КП становится `rejected`; `pickActiveQuote` без
accepted и без sent берёт последнее любое — то же самое КП. Карточка рисует
«Отклонено 01.09.2026». Читается как дата отказа, фактически это дата отправки.
Исключения нет, в консоли пусто — дата просто неверная.

## Правка

Файл `src/components/projects/ActiveQuoteCard.tsx`, объявление `stampedAt`.

Найти (вместе с комментарием над ним — он тоже уходит):

    // Дата под статусом: у отправленного — когда отправили, у принятого — когда приняли.
    // `sent_at`/`accepted_at` — timestamptz, поэтому formatDateNumeric; `valid_until` —
    // колонка `date`, и ей нужен formatCalendarDate (иначе сутки назад в минусовых зонах).
    const stampedAt =
      active.status === 'accepted' ? active.accepted_at : active.sent_at;

Заменить на:

    // Штамп показываем ТОЛЬКО там, где он есть в схеме: `stamp_quote_status` (053)
    // пишет `accepted_at` и `sent_at`, и больше ничего. Момент отклонения в базе не
    // хранится, а `updated_at` им не является — он сдвинется на любой следующей правке
    // КП. У `rejected`/`draft`/`expired` даты под статусом нет вовсе: пусто честнее,
    // чем чужая дата под чужой подписью.
    //
    // `sent_at`/`accepted_at` — timestamptz, поэтому formatDateNumeric; `valid_until` —
    // колонка `date`, и ей нужен formatCalendarDate (иначе сутки назад в минусовых зонах).
    const stampedAt =
      active.status === 'accepted' ? active.accepted_at
      : active.status === 'sent' ? active.sent_at
      : null;

Прежний комментарий описывал две ветки из трёх — ровно поэтому третья и проехала.
Новый обязан перечислять все.

## Чего НЕ делать

- Не заводить `rejected_at` и не трогать триггер `stamp_quote_status`. Решение владельца
  на гейте: колонка добавилась бы дёшево, но осмысленной её делает правка
  `SECURITY DEFINER`-триггера, а это дороже подписи в одном виджете. Появится срез
  «когда и почему отваливались КП» — заведёт своя миграция.
- Не подставлять `updated_at` вместо штампа: он сдвигается на любой правке КП.
- Ничего, кроме этого объявления и его комментария, не менять. Разметку строки статуса,
  `formatDateNumeric`, `quoteValidity` не трогать.

## ТЕСТЫ

Тестов нет: правка в компоненте, новой чистой функции не появилось. `stampedAt` —
тернарник по трём статусам, юнит на него тестировал бы разметку.

## ФИНАЛЬНАЯ ПРОВЕРКА

    npx tsc --noEmit && npm run lint 2>&1 | tail -5 && npx vitest run 2>&1 | tail -5 && npm run build 2>&1 | tail -10

Плюс глазами: у отправленного КП дата под статусом осталась, у отклонённого её нет.

## КОММИТ

Отдельным коммитом поверх `890849d`, НЕ amend — гейт уже читал тот коммит, история
правки должна остаться видимой.

    fix(deals): дата под статусом КП только там, где штамп есть (гейт F3)

    Карточка активного КП печатала sent_at под подписью «Отклонено» — дата отправки
    читалась как дата отказа. Момент отклонения в схеме не хранится, поэтому у
    rejected/draft/expired даты под статусом теперь нет вовсе.

**Не мержить.** Отчёт — на гейт.
