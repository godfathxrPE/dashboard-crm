# Гейт S-DEAL-STAKE-VIEW-1 — PR #137 (`b9c931b`)

**26.09.2026.** Работа `304054f`, squash → `b9c931b`. Прод `dpl_3Be9B39W5USDNHjeTZahwpKwGrL3` (READY),
откат — `dpl_HFeL9KXFuBfR65Z4N7uQ7ajEhsrr` (`1df1dc0`). Миграций нет. Спринт-файл — #136.

## Сделано
- Человек — карточка: аватар с инициалами (оттенок по имени, мягкий тинт), кольцо `outline` у
  основного контакта вместо звезды и тега «основной», подстрока «основной контакт · должность»,
  телефон (`formatPhone`/`telHref`) и почта (`mailto` + копирование) отдельными строками, тег —
  записанная роль или «указать роль».
- Пустой слот — пунктирная карточка с подсказкой текстом; обязательная роль — `text-danger-text`,
  текст `ROLE_MISSING_DETAIL` (одна константа с сигналом `single_threaded` в «Сводке», тест-страж).
- `avatar.ts`: `getAvatarTone` — тот же оттенок, что `getAvatarColor` (тест); поведение компании и
  контакта прежнее.
- Закрыты **P-1** (разделители дугой — стопка карточек с `gap`) и **G-3** в «Стейкхолдерах»
  (`[@media(hover:none)]:opacity-100`).

## Отступления — приняты
1. Кольцо primary — `outline`, не `box-shadow`: в minimal тень пропадала на тонированном аватаре.
2. Аватар — мягкий тинт + цветные инициалы: белые на жёлтом и лаймовом не читались.
3. Карточка в две строки, контакты и действия на всю ширину: в рельсе 320 px почта ломалась в три строки.
4. Подсказка обязательного слота — только объяснение, без «ЛПР не в контуре»: роль названа заголовком.

## Проверка
- Дифф: один коммит, 5 файлов; общих файлов с ACTIVITY-VIEW-1 нет.
- tsc 0, lint 0 ошибок, vitest 2093 зелёных, build зелёный (отчёт исполнителя; CI `checks` зелёный).
- Линзы: базовая ✓ · дизайн ✓ (статичная карта классов тона, hex/rgba нет) · a11y ✓ (действия видны
  на таче) · security/api/supabase — n/a. Cold review не требовался.
- Визуально исполнителем: две живые сделки, 8 тем. Не проверено вживую: роль viewer, реальный тач.
