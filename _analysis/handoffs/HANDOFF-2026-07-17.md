# Handoff — 2026-07-17 (S-QUOTE-1 запушен, миграция 053 в проде; открыт только браузерный смок «КП»)

## Как читать
Свежая сессия: этот файл первым, затем `claude/s-quote-1-gate.md`, `claude/s-wbs-1-gate.md`, `claude/sprint-S-WBS-1.md`, `claude/s-portfolio-2-gate.md`. **Живая ФС на Маке — источник правды**, docs могут отставать (см. Memory-gap).

## Рабочая модель (не менять)
- **CC** (Claude Code на Маке) пишет+коммитит+**пушит**. **Cowork** готовит промпты, гейтит по живой ФС, применяет Supabase-миграции (MCP), смокает через Chrome. **Grok** ревьюит (`_analysis/review-*.md`).
- Репо: `/Users/oleg/Downloads/dashboard-crm` (в device_bash монтируется как `mnt/dashboard-crm`). Стек: Next 15 + TS strict + Tailwind (6 тем) + Supabase + Netlify.
- **Прод URL: `https://rococo-quokka-4ae212.netlify.app`** (Netlify siteId 9225ea47-42a3-4f06-ae75-fdefe796971e; в репо URL нет — только siteId). Netlify auto-deploy из GitHub push. Deploy-статус через MCP недоступен (Netlify MCP тут = только design-import) → проверять деплой навигацией на прод-URL.
- **Прод Supabase ref: `uoiavcabxgdjugzryrmj`**. Миграции применены **по 053** (052_task_wbs применена гейтом Cowork 2026-07-17). Правка данных/миграции на проде — через Supabase MCP (`execute_sql` / `apply_migration`), данные — с capture→restore. NB: 047 = `047_drop_legacy_projects_stage` **применён** (в schema_migrations), файл убран из папки — не «пропущен».
- Безопасность: force-push/drop table/прод-миграции — только с явного подтверждения. Пуш в main = деплой прод = решение пользователя.
- Разведку API — по живой ФС (device_bash; `device_stage_files` иногда STALE). Не гадать про API примитивов — читать исходник.

## Git-состояние
- **origin/main = `b483b79`** — S-QUOTE-1 запушен пользователем (сказал «запушил» в конце сессии 2026-07-17): `9a79442` (S-QUOTE-1 feat) + `b483b79` (docs/schema.md quotes). Netlify подхватил деплой. **⚠️ независимо не сверено** — если нужно точно, проверить origin/прод при следующем контакте с ФС/прод-URL.
- S-WBS-1 (`1339640`) — запушен ранее, в проде (Netlify + прод-смок PASS). S-QUOTE-1 сверху него fast-forward.
- **Прод Supabase: миграции по 053** (053_quotes применена гейтом Cowork 2026-07-17, объекты + W4/W5 верифицированы). 052 (WBS) тоже в проде.
- ⚠️ **Git через device_bash — грабля бриджа:** нет сети (пушить нельзя) + не удаляет файлы → stale `.git/index.lock` после каждой git-команды (убирал через `mv` в `_to_delete/`). Коммит/пуш — на CC (нативный git). Не гонять git через device_bash без нужды.
- Рабочее дерево (НЕ коммитить): `tsconfig.json` M, `_analysis/*`, `.grok/`, `_to_delete/` — локальные твики/зеркала/мусор.

## Что закрыто и ПРОВЕРЕНО В ПРОДЕ (2026-07-17)
- **S-QUOTE-1** (КП на сделке) — коммиты `9a79442`+`b483b79` **запушены** (по слову пользователя), миграция 053 применена+верифицирована (W4 partial-uniq accepted, W5 stamp — функц-тест PASS). `quotes`→projects client (CASCADE), amount=копейки, lifecycle draft→sent→accepted→rejected→expired, вкладка «КП» client-only, accept→budget под ролевым гейтом. Grok 8.5/10 (W1–W8 вшиты), гейт+build PASS. **⚠️ ЕДИНСТВЕННЫЙ ОТКРЫТЫЙ ПУНКТ: браузерный смок вкладки «КП» + accept→budget — НЕ выполнен** (в прошлой сессии дошли до открытой модалки «Новое КП» на сделке `aa2c3628-8020-4637-898c-537b37120e6a` «ЭЙЧ ЭНД ЭН», ничего не создали/не мутировали; смок прервали). Детали + шаги смока: `s-quote-1-gate.md`.
- **S-WBS-1** (иерархия задач) — коммиты `04a6b1d`+`1339640` **в проде**, миграция 052 применена, прод-смок PASS. `tasks += parent_task_id (on delete set null) + wbs_code`; триггер `check_task_parent_valid` (self/cross-org/cross-project/цикл, паттерн 048) + `orphan_children_on_project_move` (W4). Модалка: parent-select (same project, anti-cycle) + wbs-инпут. Gantt: сводный скобка-бар (span=обёртка детей) + depth-отступ + свёртка (chevron); `usePortfolioHealth`-стиль общий хук не трогали. Grok 8.5/10 (W1–W8 вшиты), Cowork-гейт + Chrome-смок PASS (дерево/свёртка/backward-compat; DB-триггер 23514). **F1 (нюанс, не блокер):** undated-родитель не роллапится — сводным становится только с собственными датами (отклонение от спеки; решить: принять v1 или S-WBS-1.1). Детали: `s-wbs-1-gate.md`. **Хвост:** gen types + снять overrides в `entities.ts` (на Маке через CC); docs/schema.md + skill дельта 052.
- **S-PORTFOLIO-2** (risk-виджет на /overview) — коммит `f398209`, прод-смок PASS. Виджет `PortfolioRiskWidget` под KPI: «N в зоне риска» + топ-N красных, zero-state (зелёный), deep-link `/projects?tab=portfolio`. Таб «Портфель» переведён на URL (`?tab=`, `<Suspense>` в page). Общий хук `usePortfolioHealth` — единый источник ранжирования (PortfolioView + виджет). Nit S-PORTFOLIO-1 (direction=null бейдж) закрыт этим же спринтом. Гейт (tsc exit 0, инварианты) + localhost-смок + **red-path E2E** + прод-смок — всё PASS. Детали: `s-portfolio-2-gate.md`.
- **Red-path верифицирован E2E** (закрывает «не проверено визуально» из S-PORTFOLIO-1): временно Test1 → deadline вчера + stage_entered_at −40д → score 20 at_risk; виджет показал «1 в риске» + строку с причиной + score, портфель — красное-сверху + колонка «Причины» + красный дедлайн + dwell жёлтым. **Откат выполнен** (deadline=null, stage_entered_at восстановлен; виджет снова зелёный). Остаток: `updated_at` Test1 = 2026-07-17 (триггер `moddatetime`, на health не влияет).
- **S-PORTFOLIO-1** (таб «Портфель» /projects) — в проде (прошлый батч). Health-ранжирование, риск-счётчики, старение по фазам.
- **Workflow-эпик D1 task_overdue UI (2C-B)** + **Supabase 050+051** — в проде (прошлый батч, см. историю ниже по гейтам).

## Ближайший шаг (СТАРТ новой сессии) — выбор пользователя
S-QUOTE-1 (#9) закрыт по коду и миграции, **открыт только браузерный смок «КП»** (шаги — в `s-quote-1-gate.md` §Осталось). Открыт также **F1** (S-WBS-1): undated-родитель не роллапится — принять v1 или S-WBS-1.1. Следующий эпик по §13 (undone): **S-SPAWN-UX-2** (#11, превью шаблона при spawn + сдвиг дат) или **S-CHECKLIST-1** (#12, sign-off чеклисты ДО). Плюс кандидаты вне порядка:
- **confirm-эпик**: `window.confirm` → `useConfirm` (25+ мест) — накопленный UX-долг.
- **crm-architect skill sync** (уточнено 2026-07-17): reference-файлы АКТУАЛЬНЫ — `schema.md` до 001–046 + Волна 2, `theme-system.md` = 6 тем / дефолт `t-aura` (scandi удалён). Устарела только **сводная таблица «Project Identity» в `SKILL.md`** (`001–026`, `Scandi`, `9 тем`) — рассинхрон с собственными reference. Что доделать: (1) поправить таблицу в SKILL.md; (2) `schema.md` не покрывает 047–053 (workflow/task_deps/task_overdue/wbs/quotes); (3) `architecture.md` не знает про `usePortfolioHealth`, `PortfolioRiskWidget`, `PortfolioView`, паттерн «таб → `?tab=` + Suspense».
- Следующая фича по `improvements/CRM-ROADMAP-projects-deals.md` (сверять по живой ФС).

## Backlog
- **crm-architect skill sync**: правка сводной таблицы SKILL.md + 047–053 в schema.md + S-PORTFOLIO-компоненты в architecture.md. Reference-файлы в целом актуальны — см. «Ближайший шаг».
- **confirm-эпик**: `window.confirm` → `useConfirm`.
- **N2 a11y-nit**: декоративный `◐` в счётчике «внимание» виджета без `aria-hidden` — в косметический заход.
- **W4-note (S-QUOTE-1)**: CHECK `type='client'` на quotes — сейчас только UI-гейт (FK не CHECK'ает тип); trigger-guard при желании.
- **Memory-gap**: `claude/wave2-progress.md` не отражает health/Deal-Hub/portfolio (untracked-сессии). «Что осталось» сверять по роадмапу + живой ФС.

## Ключевые технические факты
- **Портфель-связка (единый источник, переиспользовать):** `usePortfolioHealth()` (`src/lib/hooks/use-portfolio-health.ts`) → `{ rows, counts, aging, isLoading, error }`. Внутри: `useDeliveryProjects` + `usePipelineStages` + `getDeliveryHealth`. Owner НЕ считает (маппит PortfolioView). Строки предсортированы `score asc` (краснее-сверху), терминальные исключены. И PortfolioView, и `PortfolioRiskWidget` едят его — **пороги health не форкать**.
- **Health-функция:** `getDeliveryHealth({progress_done, progress_total, stage_entered_at, deadline, updated_at, isTerminal})` + `isDeliveryTerminal(stage, status)`. Score 0..100 (healthy≥75/attention≥40/at_risk<40), терминал→100. Штрафы: overdue −40, застой(>30д) −25, тишина(>14д) −20, низкий прогресс(<30% при dwell>14д) −15.
- **Таб-URL паттерн:** активный таб = searchParams (`?tab=`), `setTab` через `router.replace(scroll:false)`, дефолт = чистый URL. `useSearchParams()` требует `<Suspense>` в page (иначе билд-ошибка). Референс: `ProjectsSection` + `app/(dashboard)/projects/page.tsx`.
- **DataTable-грабля:** сортирует `String(item[col.key])`, старт `sortKey=null`. Числовые колонки НЕ sortable, массив предсортировать; sortable-ключи на верхнем уровне row.
- **Delivery-фазы:** `DELIVERY_PHASE_ORDER=['initiated','planning','execution','completed']`, `DELIVERY_PHASE_TEXT` — var-токены.
- **projectHref:** delivery/internal → `/projects/[id]`, client → `/deals/[id]`.
- **Quotes (053):** `quotes` → projects (client, ON DELETE CASCADE), `amount bigint` = копейки (UI-ввод рубли через `parseBudgetInput`), currency default RUB, lifecycle enum `quote_status`(draft/sent/accepted/rejected/expired). W4 partial-uniq `(project_id) WHERE status='accepted'` (один accepted на проект). W5 trigger `stamp_quote_status` авто-стемпит sent_at/accepted_at. Вкладка «КП» client-only. accept→budget под `canUpdateDealBudget` (owner/admin/владелец сделки).
- **Токены/темы:** zero hex, var-токены (`surface`/`accent-l`/`surface-hover`/`text-red/yellow/green/mute/dim/main`), `tabular-nums`, CVD-глифы ●/◐/▲. Темы: aura/washi/fuji/frost/aurora/tidal. Note: `text-[10px]`-стиль px-размеры — устоявшаяся конвенция репо (не считать нарушением «no px»).
- **projects: нет `deleted_at`** (soft-delete не на этой таблице). `updated_at` — триггер `moddatetime`.