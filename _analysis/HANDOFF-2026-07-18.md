# Handoff — dashboard-crm, сессия 2026-07-18

Роль ассистента в сессии: **PM-ревьюер + гейт Cowork** (пишет спринты, ревьюит diff, применяет миграции через Supabase MCP, смокает, визуально проверяет прод в Chrome). Claude Code (на машине Олега) пишет код и коммитит, **не** применяет миграции.

## TL;DR состояния
Глубокое ревью CRM (код + визуал) → план из 4 волн → выполнено **W1, W2, T1a, T2** как гейтированные спринты. Закрыта вся тяжёлая инженерия: дыры RLS, враньё времени/цифр, сломанный приём инвайта, невидимое авторство + эскалация ролей. Онбординг команды работает (доказан на проде). Дальше — только улучшения (масштаб/полировка), ничего блокирующего.

## Координаты
- **Прод:** https://rococo-quokka-4ae212.netlify.app (Torii CRM v4.0). Netlify auto-deploy из `main`.
- **Supabase:** ref `uoiavcabxgdjugzryrmj`. Org Олега: `f063c8ea-7a7a-467b-a2ae-36e7b43e0cce`.
- **Стек:** Next.js 15 + React 19 + TS + Tailwind + Supabase + TanStack Query. Локальный путь `~/Downloads/dashboard-crm`.
- **Юзеры:** `mzk.oleg@gmail.com` (owner), `god4azer@gmail.com` (manager — расстопорен в этой сессии).

## Сделано и в проде

### W1 — Безопасность (commit 9efeb56 + хвост 488f3af, pushed)
- **054** `rls_update_with_check` (v20260717215536) — WITH CHECK org_id на всех UPDATE-политиках без него (calls/companies/contacts/leads/meetings/projects/tasks/quotes + organizations/profiles) + `freeze_org_id()` триггер `trg_aa_freeze_org_id` на 29 таблицах с org_id. Закрыл межтенантный перенос строки.
- **055** `storage_project_files` (v…215622) — политики бакета project-files в git, private, 50MB.
- **056** `revoke_anon_defaults` (v…215630) — REVOKE anon (default privileges + all tables).
- **056b** `revoke_trigger_fn_execute` (applied на гейте) — снят EXECUTE с чисто триггерных DEFINER-функций.
- Код: netlify.toml HSTS+CSP-lite, `safeHref` на do_url/document_url, дедуп `ai-summarize` (429 если <10 мин).
- Смок: tamper org_id под authenticated → строка осталась в родной org; owner-reassign проходит; anon_any_table=0.

### W2 — Правда во времени и цифрах (commit 3cb80b5 + 0ae5940/e0d45ba, pushed)
- **057** `backfill_datetime_tz` (v20260718085820) — разовый −3ч сдвиг всех pre-deploy timestamptz (данные тестовые → сдвинули всё, включая import-партию). **Файл нейтрализован** (закомментирован + баннер «APPLIED, не запускать» — сдвиг не идемпотентен).
- Код: пара конвертеров `datetimeLocalToIso/isoToDatetimeLocal` (единый путь запись/чтение datetime-local); единый `deal-metrics.ts` (active=client+open, conversion/pipeline — один источник, срезы подписаны); один `describeEvent` с FIELD_LABELS (лента без сырых `stage_id`); optimistic задач патчит все срезы `['tasks']` (доска/Гант не прыгают); удалён мёртвый `currency.ts`.
- Проверено на проде: звонок 13 июл. показывает 15:54 (было 18:54); Обзор↔Сделки согласованы; лента человекочитаема.

### T1a — Приём инвайта (commit 5846c55, pushed)
- **058** `accept_invitation` (v20260718102123) — RPC `accept_invitation(token)` SECURITY DEFINER (обходит RLS memberships), токен-flow. ACL: authenticated only.
- Код: страница `/invite?token=`, org-guard в `middleware.ts` (org-less → /invite, не пустой дашборд), next-round-trip (login→callback→middleware, open-redirect guard), токен-ссылки (`inviteLink(token)`, createInvitation `.select('id,token')`).
- **Доказано на проде:** `/invite?token=` → «Вы добавлены в организацию» → редирект в дашборд. Смок RPC: accepted / invalid(идемпотент) / expired / unauthenticated.
- **Модель писем (важно):** T1a **не шлёт письма**. Инвайт = ты копируешь ссылку и отправляешь сам. Login-magic-link шлёт Supabase (встроенная почта работает, ~пара/час, спам-риск).

### T2 — Корректность команды (commit ab88f24 — ⚠️ НЕ ЗАПУШЕН)
- **059** `membership_role_guard` (v20260718112617, **applied на гейте**) — admin не трогает owner: `membership_update` USING (старая роль) + WITH CHECK (новая роль), admin ограничен manager/viewer; `membership_delete` тот же USING + self-delete. Смок: admin разжаловать/удалить owner → заблокировано; manager→viewer/delete → ок; эскалация manager→admin → 42501.
- Код (ждёт пуша): актор в ленте (`use-actor.ts`, «• Имя» в EntityTimeline/DashboardHome/ActivityDrawer + created_by/user_id в селектах); `stage_changed {from/to_name}` вместо обезличенного project_updated + Set-дедуп лейблов; роль-гейт create на Companies/Contacts/Calls/Leads/Meetings + CommandPalette + CompanyDetail; TeamSection прячет удаление/смену роли owner-строки для не-owner.

## Ожидает действий Олега (не код)
1. **Запушить T2** (`ab88f24`) → Netlify задеплоит фронт. 059 уже в проде (безопасно: owner не задет).
2. **Онбординг:** разослать 2 готовые invite-ссылки (в чате) — `nacccsgo@gmail.com`, `filipasisanastasio@gmail.com` (pending, живут до 1 авг). Дать команде поработать неделю → сигнал для след. спринта.
3. **Поправить память CC:** он не видит MCP-applies → считает 057/058/059 непринятыми. Статусы: 057=20260718085820, 058=20260718102123, 059=20260718112617 — **applied, не применять повторно.**
4. Мелочи в CC: регенерить типы (снять gen-стаб `accept_invitation`), дописать docs/schema.md по 058/059.

## Бэклог (отложено, с причинами)

**Готовые спринт-промпты в `_analysis/` (v2, отревьюены grok):**
- **W4a — дешёвая перфа** ← лучший кандидат, если катать до сигнала. Dynamic import xlsx/Gantt/charts + чистка шрифтов + prefetch на 3 экрана. Убирает 3–5с спиннеры. Низкий риск, виден каждый заход.
- **W4b — порядок.** Распил ProjectDetail (912 строк), user-scope localStorage + единый «фокус дня», focus-trap Modal, «Сегодня» разгребание (snooze/bulk), порог «Остывают» (только с ≥1 касанием), мёртвый код, lint-in-build.
- **W3 — масштаб.** org-fetch → count/пагинация, ленивый серверный ⌘K, view `contact_last_touch` (**миграция 060**), error-бейджи. Преждевременно на тестовых данных; главный regression-риск (infinite-query optimistic).
- **T1b — авто-письмо инвайта** (Resend/SMTP). Отложен: встроенная почта работает, копипаст-ссылка достаточна. Нужно решение по инфре.

**Хвосты аудита (daily-use качество, «later»):**
- Тосты на назначение (sonner ↔ realtime — сейчас только бейдж колокольчика).
- Ответственный (`owner_id`/`assigned_to`) для звонков/встреч/лидов — «чей звонок» не назначить.
- Уведомления на смену owner компании/контакта и смену стадии (кроме won).
- Per-user настройки: `user_settings` (таблица есть, приложением не используется) или user-скоуп localStorage (`saved-views`/`theme`/`focus` — сейчас общие в браузере, не чистятся при логауте).
- Онбординг новичка: welcome/пустые состояния с CTA (`EmptyState` есть, нигде не подключён).
- Роль-чек в `leads_insert_own` (viewer может создать лид — рассинхрон).

**Инфра/конфиг (Supabase Dashboard, не код):**
- Custom SMTP в Auth → надёжная доставка magic-link при масштабе (не блокер сейчас).
- Redirect URLs должны пускать `.../callback` c `?next=` (проверить при первом реальном заходе по ссылке).
- Leaked-password protection — тумблер (minor).

**Мелочи:**
- `/invite` держит «Принимаем…» ~8с до редиректа — делать `replace('/')` сразу, инвалидацию параллельно.
- middleware зовёт `current_org_id` RPC на каждый запрос — позже в JWT-claim (`app_metadata`).

## Гочи сессии (для памяти)
- **CC не видит MCP-applies** (он на машине Олега, БД через мост не читает) → после каждого гейт-apply правит память вручную, иначе повторно «применит» (ловили на 057 — чуть не было двойного −3ч; на 058/059).
- **git через Cowork-мост** оставляет несносимый `.git/index.lock` (mount не может unlink). Нативный git у CC не задет. Гейт git не трогает — коммиты/пуш делает Олег/CC.
- **Нумерация миграций:** 054–059 заняты; W3 `contact_last_touch` переномерован на **060**.
- **Гейт-контракт (работает, держать):** CC пишет миграцию + коммитит, НЕ применяет. Cowork: ревью diff → `apply_migration` → смок симуляцией ролей (`set request.jwt.claims` + `set local role authenticated`, транзакция с rollback) → `get_advisors` → визуал в Chrome. Остаточные advisor-WARN — легитимные DEFINER-RPC + leaked-password (норма).

## Артефакты сессии (`_analysis/`)
- `REVIEW-2026-07-18-senior-pm.md` — исходное глубокое ревью (P0/P1/P2).
- `TEAM-READINESS-2026-07-18.md` — аудит готовности к команде + план T-волны.
- `sprint-w1-security.md` … `sprint-w4-speed-order.md`, `sprint-t1a/t1b/t2.md` — спринт-промпты (v2 с фиксами grok).
- `review-sprint-*.md` — ревью grok по каждому спринту.

## Рекомендация на выход
Большие волны закрыты. Дальше по плану — **не спринт, а живое использование:** пуш T2 → онбординг 2 человек → неделя реального юза → следующий спринт по факту фрикции. Хочешь катать параллельно — **W4a** (дешёвая перфа) единственный оправданный до-сигнальный кандидат.
