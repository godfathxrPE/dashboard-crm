# Гейт S-STAGE-PROFILE-1 — PR #117 (`c988e1b`)

**24.09.2026.** Работа: `6b8753e` (карта) + `eaf8c97` (fix палитры), squash → `c988e1b`.
Спринт — `_analysis/sprint-S-STAGE-PROFILE-1.md`, fix — `_analysis/fix-S-STAGE-PROFILE-1-palette.md`.
Миграций нет, запросов нет.

## Итог

Принято. Карта в проде, палитра переделана до мержа по приёмке владельца.

## Что проверено

- Дифф `6b8753e` (7 файлов, +1300/−13): `PipelineCockpit` не тронут, профиль в слоте `map`;
  `handleStageClick` прежний — назад через `onRollback` и подтверждение `ProjectDetail`, вперёд
  через `openTransition` с гейтом требований; меняется только вызывающий (кнопка строки деталей).
- Тесты: `tests/unit/stage-profile.test.ts`, 19 кейсов поведением.
- Регрессия #108 подтверждена чтением истории: `git show 9db5ff0^:globals.css` — блок
  `.t-washi { --mark-today: var(--text) }` был, в `602c393` — нет. Восстановлен после `:root`.
- Дифф `eaf8c97` (2 файла, +28/−7): ровно шесть мест заливок/легенды; контуры, числа,
  `TONE_*`, `historic`, `muted` не тронуты. Два расхождения проверок fix-файла — ошибки файла
  (7 объявлений, а не 8; третье совпадение `grep` — узел «пройдено», не заливка).
- `audit-contrast.py` — ровно 3 FAIL (nav-active frost/aurora/tidal), как до спринта.
- Прод `c988e1b`: «Глорус-норд» в lime/washi/frost/aura, тема — классом в DOM без записи
  в localStorage, transition отключён на время замера. Текущая стадия самая заметная;
  пересвет прошлой — тинт; «следующая» — пустой контур; белых/жёлтых плит в frost нет.
- Линзы: базовая ✓ · design ✓ (8 тем у исполнителя, 4 на проде; токены вместо hex) ·
  a11y ✓ (`<button>` + `aria-expanded/controls/current`, Escape, focus-visible) ·
  security — n/a · api — n/a · supabase — n/a (запросов нет, `useStageStory` тем же ключом).

## Находки

- 🔴→исправлено до мержа: палитра «вырви глаз» на всех темах. Причина — в спеке, не в
  исполнении: токены 100% подбирались под макет, где пересвет — полоска. Правило в learnings.
- 🟡 P-5: выброс прошлой стадии задаёт масштаб и сжимает текущую («ЭЙЧ ЭНД ЭН», «Лид» 62/2).
- 🟡 washi: текущая «в норме» = `--text` — самая тяжёлая плита.
- 🟡 P-4 (до спринта, стал заметнее): до загрузки org settings карта рисует нормы по умолчанию.
- 🟢 Закрытая сделка: столбики не кликаются (спека «locked»), заходы — только во вкладке «История».

## Процесс

- Регрессию #108 пропустили два гейта: #108 (восемь тем глазами — но «сегодня» на таймлайне
  не смотрели) и сверка входа #115 (проверено «accent = red в washi», не проверено, что
  переопределение на месте).
- Fix-файл содержал два неверных ожидания проверок — исполнитель их поймал и не стал
  «подгонять» код под ожидание.

## Verification

```
Type Safety:            PASS (tsc чистый у исполнителя, дифф прочитан)
RLS Coverage:           NOT_APPLICABLE
Backward Compatibility: PASS (StageRail лида и PipelineCockpit не тронуты)
Runtime Tested:         PASS на проде (4 темы, 1 сделка); 8 тем × 2 сделки — у исполнителя
Regional Availability:  NOT_APPLICABLE
```
