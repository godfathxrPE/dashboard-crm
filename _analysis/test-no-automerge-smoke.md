# Смок DOCS_PR_NO_AUTOMERGE (2026-09-07)

Одноразовый файл для проверки шага 7 `scripts/docs-pr.sh`: при `DOCS_PR_NO_AUTOMERGE=1`
скрипт не должен вызывать `gh pr merge --auto`. PR закрывается руками сразу после
проверки, без мержа — этот файл проверки не переживает.
