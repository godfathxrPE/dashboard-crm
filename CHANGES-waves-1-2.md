# Волны 1–2: что сделано и что проверить

Период: июль 2026. База: CRM-EVOLUTION-PLAN.md (бенчмарк Attio / Pipedrive /
Close / folk / Linear). Семь спринтов, все закоммичены, tsc = 0, билд чистый.

---

## Что сделано

### Волна 1 — механика сделки

**W1a · `0d21e90` — Always next action + rotting indicator**
- Миграция 017 (применена к Supabase): `projects.next_action_date`,
  `projects.pinned_note`.
- `getDealHealth()`: ok / нет шага / шаг просрочен. Гниют только
  `status === 'open'` (on_hold не трогаем).
- Индикация: kanban-карточки и таблица — контурная ○ (нет шага/даты, yellow),
  заполненная ● (просрочен N дн., red). Различие формой — работает в
  монохромном Scandi.
- Поле «Дата шага» в ProjectModal; мягкий toast «Запланируй следующий шаг»
  при переносе сделки между стадиями (только Воронка, не StageBoard).

**W1b · `c6f4d40` — Экран «Сегодня» (action inbox)**
- `/` — очередь действий: Фокус дня → Просроченные звонки → Звонки сегодня →
  Задачи в работе (просроченные сверху) → Сделки без шага → Встречи → Остывают
  (добавлено в W2b). Пустые секции скрыты.
- Каждая строка: клик — переход; primary — «Выполнен» / «Готово» /
  «Запланировать шаг»; secondary — «На завтра».
- Старый KPI-дашборд переехал на `/overview` («Обзор» в sidebar).
- Focus-виджет переехал из ActivityDrawer (оттуда убраны Focus и
  Planned Calls).

**W1c · `d04d41f` — Focus panel в карточке сделки**
- DealFocusPanel под пайплайном, над табами: следующий шаг + дата (inline
  edit), «Шаг сделан ✓» (очищает оба поля → сделка честно в rotting),
  закреплённая заметка (pinned_note, textarea), Здоровье + «N дн. без
  активности». Только для открытых сделок.
- InlineEdit получил `as="textarea"` (Cmd+Enter — сохранить).

### Волна 2 — скорость

**W2a · `92571f0` — Command palette 2.0**
- GlobalModals host: `openModal(id)` из ui-store рендерит любую модалку
  с любой страницы (задел был мёртвый — оживлён).
- Палитра: секция «Действия» (6 команд создания) + kbd-подсказки T/C/P/M
  (работают при пустом запросе) + футер-легенда. Навигация актуализирована
  (Сегодня, Обзор).
- Глобальный `N` — палитра в режиме «только Действия».
- Попутно: оживлены мёртвые кнопки поиска в Header; MeetingModal TS-ошибки
  ушли (baseline tsc = 0).

**W2b · `ae55d2e` — Reconnect («остывают»)**
- last_touch на клиенте (без миграций): только состоявшиеся звонки
  (status done) и прошедшие встречи. Порог 21 день
  (`lib/constants/reconnect.ts`), cold = 2×порога.
- Секция «Остывают» в Сегодня: только контакты активных сделок (топ-5),
  «Запланировать звонок» → CallModal с préfill контакта
  (openModal получил context: {contactId, companyId, projectId}).
- Колонка «Касание» в /contacts (sortable), индикатор в карточке контакта.

**W2c · `319a077` — Saved views**
- useChipFilter переехал в URL (`?f=key1,key2`) — фильтры переживают F5,
  share-able, history не засоряется (router.replace). API совместим.
- useSavedViews (localStorage): «Сохранить вид» с inline-именем, чипы видов
  с Bookmark-иконкой на Projects/Companies/Contacts, крестик — удалить.
- Секция «Виды» в палитре: все виды всех страниц, выбор — переход с фильтрами.

**W2d · `9a09c2f` — Keyboard nav + Peek**
- j/k (и стрелки, работает в русской раскладке через e.code) во всех
  DataTable: Projects/Contacts/Companies/Leads. Enter — открыть,
  Space — peek, Escape — сброс. Глушится при открытых модалках/палитре/инпутах.
- PeekPanel (справа, 440px, z-40): сделка — статус + DealFocusPanel compact
  (правка шага прямо из peek) + компания/контакт + 3 события таймлайна;
  контакт — должность/компании/связь/касание. Peek следует за j/k.
- «Сегодня»: j/k/Enter + `d` — primary-действие строки.
- Подсказки в футере палитры и help-оверлее (?).

---

## Чек-лист ручной проверки

Прогнать в t-scandi и одной тёмной (t-frost/t-tidal). Dev-сервер после
обновления веток перезапускать (`rm -rf .next && npm run dev` при вечной
загрузке — случалось дважды).

### Цикл сделки (W1a/W1c)
- [ ] Kanban: сделка без шага — ○ жёлтая метка; с просроченной датой — ● красная; закрытая — без меток
- [ ] Перетащить сделку между стадиями → мягкий toast «Запланируй следующий шаг» → кнопка открывает модалку с фокусом на дате
- [ ] Карточка сделки: правка шага/даты inline сохраняется; «Шаг сделан» очищает поля и сделка появляется в «Сегодня → Сделки без шага»
- [ ] Заполнить даты шагов у своих реальных сделок (иначе всё жёлтое)

### Сегодня (W1b/W2b)
- [ ] `/` открывает очередь; счётчик «N требуют действия» сходится с суммой секций
- [ ] «Выполнен» у звонка / «Готово» у задачи убирает строку сразу (optimistic)
- [ ] «На завтра» переносит дату
- [ ] «Остывают»: только контакты живых сделок; «Запланировать звонок» подставляет контакт
- [ ] Пустая очередь → «Всё разобрано» + ссылка на Обзор

### Палитра и создание (W2a)
- [ ] ⌘K с любой страницы; T/C/P/M при пустом запросе открывают модалки
- [ ] `N` — режим «только Действия»
- [ ] Esc-цепочка: закрывает модалку → палитру → ничего не ломает
- [ ] Создание сущности из палитры реально сохраняет запись

### Виды и фильтры (W2c)
- [ ] Чипы фильтров меняют URL; F5 сохраняет выбор; back-button не завален
- [ ] «Сохранить вид» → чип появляется, активный подсвечен
- [ ] Вид из палитры с другой страницы возвращает с фильтрами
- [ ] Удаление вида крестиком

### Клавиатура и peek (W2d)
- [ ] На таблице: j/k ходит (после наведения мыши на таблицу — известный шов, см. ниже), стоп на краях
- [ ] Space — peek; j/k при открытом peek меняет содержимое; правка шага в peek работает; Esc закрывает
- [ ] Ввод в поиске таблицы НЕ дёргает навигацию
- [ ] «Сегодня»: j/k + `d` выполняет primary-действие
- [ ] В русской раскладке j/k/d работают

---

## Известные швы (не блокеры, кандидаты в мелкий спринт)

1. **Keyboard nav спит до первого hover по таблице** — если DataTable на
   странице одна, активировать по умолчанию (однострочный фикс).
2. Палитра: первый пункт секции «Виды» при пустом запросе подрезается
   футером — добавить padding-bottom списку.
3. Reconnect préfill заполняет контакт, но не компанию — передавать оба
   из context.
4. Палитра не переживает навигацию, если открыта в момент route change
   (гонка, почти невоспроизводима руками) — сброс commandPaletteOpen
   на route change.
5. TodayView: «На завтра» видна только на hover — вернуться при живой обкатке.
6. Drop-prompt отсутствует в StageBoard («Доска») — сознательно, решить
   после обкатки в Воронке.

## Следующее решение

Обкатка 1–2 недели на живой работе, затем: концепт «Мостик» (command-first
визуал; фундамент — W2a/W2d — готов) или точечные фиксы из списка швов
по накопленным ощущениям. См. CRM-EVOLUTION-PLAN.md, Волна 3 (AI-слой) —
после.
