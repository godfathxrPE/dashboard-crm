# STATUS — dashboard-crm

**Ревизия 44 · 2026-09-08 · main: `1859bd5` (PR #85) · тег: `v2026.08.1`**

Единственный носитель состояния «что открыто / что закрыто». Правится только этот файл;
события (спринт-отчёты в `_analysis/`, гейты и хендоффы в Claude Project) неизменяемы
и своих списков долга не ведут — только ссылку сюда. Сверка с git: `scripts/status-check.sh`.
Протокол обновления — SKILL.md, раздел «Протокол закрытия спринта».

## В работе

**Пусто.** S-DEAL-ORG-1 закрыт (#83 работа, #84 хвост типов): виджет W4 собран,
**миграция 131 `quotes.rejection_reason` применена гейтом 2026-09-08** (`20260908061745`),
типы регенерированы, временный `QuoteRejectionPatch` снят, колонка вернулась в `QUOTE_COLS`.
Cold review 80/100 FIX: F3 (дата отправки под подписью «Отклонено») поднята гейтом до
блокера и исправлена до мержа, F1 (инвариант «без причины не отклонить» держится только на
новой кнопке) принята в очередь как **S-DEAL-ORG-2**. Следующий из очереди эпика запускать
с чистого main.

**Очередь (файлы готовы, не запущены):** `_analysis/fix-NAV-CARRIER.md` — скрипт не видит
левый бордер как носитель состояния (долг ниже).

**Очередь эпика «Сделка v2» — четыре спринта, все написаны** (из восьми закрыты ROLES-1 #70, PULSE-1 #71, LAYOUT-1 #80, ORG-1 #83). Источник очереди больше не
`claude/decisions-lime-theme-2026-09-05.md`, а карта `_analysis/deal-v2-gap-2026-09-06.md`,
сверяющая спецификацию макета (`_analysis/deal-v2-spec.html`, тоже под git) с кодом.
Порядок по зависимостям, не по величине.

| # | Спринт | Файл | Миграция | Зависит от |
|---|---|---|---|---|
| 1 | **S-DEAL-SUMMARY-1** — W9, сводка по спеке | `_analysis/sprint-S-DEAL-SUMMARY-1.md` | нет | — (оба блокера сняты: ROLES-1 #70, CHZ-1 #63) · два решения внутри файла |
| 2 | **S-DEAL-CHZ-2** — W11, хвосты ЧЗ | `_analysis/sprint-S-DEAL-CHZ-2.md` | нет | — (S-DEAL-CHZ-1 влит, #63) |
| 3 | **S-DEAL-DEADLINES-1** — W3, таймлайн дедлайнов | `_analysis/sprint-S-DEAL-DEADLINES-1.md` | нет | — (LAYOUT-1 влит, #80) |
| 4 | **S-DEAL-EVENT-1** — W5, «последнее событие» | `_analysis/sprint-S-DEAL-EVENT-1.md` | нет | — (LAYOUT-1 влит, #80) |

**S-DEAL-LAYOUT-1 (была строка 3) закрыта — PR #80 влит, см. «Закрыто».** Вышла из очереди
третьей за день (после ROLES-1 и PULSE-1); строки 4–6 стали 3–5, и это ПОСЛЕДНЯЯ
перенумерация с зависимостями: их больше нет.

**Очередь свободна целиком — все пять спринтов независимы и готовы к запуску.** Три
последних (DEADLINES, ORG, EVENT) ждали именно перестройки раскладки: она делалась один
раз, а они наполняют получившиеся места. Места готовы — орг. блок стоит пустым
контейнером (в него въезжает ORG-1), доска задач в стопке (под ней встаёт таймлайн
дедлайнов DEADLINES-1), ряд вкладок сокращён до четырёх.

**Развилки владельца РЕШЕНЫ 06.09** — разбор в карте, раздел «Развилки»:

- **Р-A. Вкладки против стопки → ГИБРИД.** Зона «Работа» — стопка Кокпит → Следующий шаг
  → Доска задач (таймлайн виден всегда) → Орг. блок (свёрнут в строку), под ними ряд
  вкладок для того, чего спека не рассматривала: Активность · Гант · История · Чат.
  Вкладка «КП» упраздняется, её содержимое переезжает в орг. блок. Из решения выделился
  отдельный спринт S-DEAL-LAYOUT-1 — перестройка делается один раз и до трёх виджетов,
  которые в неё встают.
- **Р-Б. Score 0–100 → НЕ ВОЗВРАЩАЕМ.** Отказ принят дважды (`calculateDealHealth` снят
  в S-HEALTH-V2-1, `deal-signals.ts` запрещает третью формулу), веса спеки никем не
  калиброваны на данных, а «48 из 100» выглядит точнее, чем есть. Кольцо остаётся счётом
  сигналов.
- **Р-В. Скрытие ЧЗ на не-IIoT → НЕ СКРЫВАЕМ, правок не требуется.** Маркировку продают
  и в ERP-контуре — это предметная область владельца, а не особенность воронки. Пустой
  блок и так не рисуется: `DealChzCard` возвращает `null`, когда профиля нет и ОКВЭД молчит.

Четвёртая развилка карты — два разных определения «полноты» (W9) — в STATUS не поднята
намеренно: она решается внутри `sprint-S-DEAL-SUMMARY-1.md` (Р-1 и Р-2), до его запуска.
Дальше за эпиком — долги из хендоффа.

**S-DEAL-CHZ-1 закрыт.** Блок «Честный Знак» встал в зону «Контекст» карточки сделки:
профиль компании (`companies.chz_groups` + `okved`) отдельным хуком `useCompanyChz`,
`PROJECT_SELECT` не тронут намеренно — тот джойн тянется в каждом списке сделок.
Дата справочника перестала быть комментарием в шапке файла: `CHZ_SNAPSHOT_DATE`
доезжает до экрана, источники — подсказкой. Миграций нет. **Хвост: визуальная приёмка
в восьми темах владельцем не делалась** — токены проверены статически (0 хардкод-цветов,
`audit-tokens.py` чист), но по хвосту S-LIME-TOKENS-1 зелёный скрипт её не заменяет.
Эпик зон карточки сделки закрыт (#53 каркас, #54 кольцо и переносы, #56/#57 доводка
кольца, #55 тон зоны «Работа»); S-DEAL-ZONES-1 из очереди снят.

**S-DEAL-DATA-1 ЗАКРЫТ — заново не ставить.** Три пункта закрыты чужими спринтами,
четвёртый ожил под своим именем как **S-DEAL-ROLES-1** (спринт 1 очереди выше). Пункт
брался из `claude/decisions-lime-theme-2026-09-05.md`, а тот составлялся по review макета,
не кода. Чем именно закрыт каждый:

- **`postpone_count`** — #54. Счёт переносов дедлайна идёт из аудита полей 087
  (`activity_log.payload.changes`), денормализованная колонка не понадобилась и в схему
  не вводится.
- **«первый консьюмер `stage_dwell_defaults`»** — консьюмеров уже два: бейдж «залипла»
  через `resolveDwellThreshold` (`src/lib/utils/deal-health.ts`, S-R2-DWELL-CFG) и
  `stage_target_days` с редактором в UI (`OrgSettingsSection.tsx`, S-STAGE-NORMS-UI-3).
- **`events.effects`** — это `activity_log.payload.changes` из 087; разбирает
  `describeEvent` (`src/lib/utils/activity-events.ts:188`), до ленты доезжает через
  `src/lib/timeline/rpc-adapter.ts`. Отдельной сущности заводить не нужно.
- **«готовность N/N»** — `src/components/projects/ProjectStageCockpit.tsx`,
  `gateItems`/`unmetCount` поверх `useStageRequirements` + `useStageGate`
  (S-PIPELINE-COCKPIT-1).

Четвёртый пункт — роли по воронке (`org_id`, `pipeline_id`, `role`) — **из «Отложено»
возвращён в очередь как S-DEAL-ROLES-1: снятие 06.09 было ошибкой разбора.** Возражение
спорило со словарём должностей — он и остаётся универсальным (`deal_stakeholders_role_chk`,
S-R2-D3 в силе). Спецификация W10 требует другого: ОЖИДАЕМЫЙ СОСТАВ КОНТУРА по типу
воронки, где пустой слот ЛПР сам является сигналом — для кольца W6 и для поля «ЛПР» в
сводке W9. Две разные сущности с похожим именем.

**Инфраструктура репозитория (06.09):** включены `allow_auto_merge` и `allow_update_branch`.
До этого автомерж был ВЫКЛЮЧЕН, и `gh pr merge --auto` возвращал
`Auto merge is not allowed for this repository`, а PR молча оставался открытым — за вечер
так накопилось три висящих PR. Вместе с правилом «ветка должна быть свежее base» это давало
цепную немержабельность: любой мерж в `main` ронял остальные открытые PR.

**Хвост S-LIME-TOKENS-1:** визуальную приёмку темы владелец делал сам — четыре дефекта
нашлись глазами уже после «0 FAIL» скрипта (фон, актив нава, разделители рельса, иконка
плитки). Пока `nav-active` не заведён в аудит, **любую новую тему принимать глазами
обязательно**, зелёный скрипт этого не заменяет.

**Хвост S-QUEUE-1:** визуальный смок очереди владелец выполнял сам, артефактов
не осталось; в `queue_snoozes` строк нет — штатно при сценарии «Отложить → Вернуть».
⚠️ При первом же странном поведении очереди проверять `j/k` НА СТЫКАХ СЕКЦИЙ: восемь
смещений `off*` считаются вручную и обязаны совпадать с порядком `<Section>` в JSX,
расхождение не ловится ни tsc, ни тестами.

**Хвост S-STAGE-STORY-1:** не проверено смоком обновление вкладки
«История» после перехода стадии без перезагрузки (realtime у `stage_transitions` нет по
построению, свежесть держит инвалидация в `useUpdateProject.onSettled`). Проверяется на
ТЕСТОВОЙ сделке: создать → двинуть стадию → посмотреть вкладку → удалить (CASCADE уберёт
переходы). Журнал живой сделки под смок не портить.

## Очередь

**🟡 S-DEAL-ORG-2 — уборка за W4, спринт НЕ написан.** Четыре пункта, полдня:
(1) **CHECK «rejected ⇒ причина» + убрать `rejected` из селекта статуса `QuoteModal`** —
находка F1 cold review. ⚠️ **Окно закрывается само:** на 2026-09-08 отклонённых КП в проде
ноль, констрейнт встаёт чисто; после первого отклонения через модалку останется только
`not valid` с ручным разбором. (2) `ActiveQuoteCard` гардит закрытую сделку по
`deal.status`, а истина — `useIsProjectActive` (`stage_id → pipeline_stages.is_won/is_lost`);
`status` там фолбэк. (3) Комментарий в `DealWaitingList` утверждает «второго запроса нет» —
верно лишь если доска уже разворачивалась; при свёрнутой это первый запрос плюс подписка
`useRealtimeSync('tasks')`. (4) `'**/.next/**'` в игнор `eslint.config.mjs`.

**🟡 Долг: `audit-contrast.py` `audit-contrast.py` не считает носителем состояния левый бордер `.nav-active`.**
Открыт #49 и НЕ является дефектом тем. Три FAIL у `t-frost`/`t-aurora`/`t-tidal`
(пилюля к рельсу 1.24 / 1.20 / 1.26) — ложные: у этих тем работает layered-дефолт
`border-left: 3px solid var(--accent)` (`globals.css:1747`), и его контраст к рельсу —
**4.96 / 4.59 / 9.97** при пороге 3.0. Состояние там опознаётся вертикальной полосой,
скрипт меряет подложку и полосы не видит.

Правка — в скрипте: состояние опознано, если выполняется ЛЮБОЕ из трёх — текст актива
различим от неактива по светлоте (текущая проверка), ИЛИ пилюля даёт ≥3:1 к рельсу,
ИЛИ левый бордер даёт ≥3:1 к рельсу. Карта «кто гасит бордер» — в `theme-system.md`.
Заготовка — `_analysis/fix-NAV-CARRIER.md`.

**Единственная настоящая слабость — `t-fuji`:** бордера нет (`border-left: none !important`),
пилюли нет (`background: transparent !important`), текст актива против неактива 1.09.
Состояние держат уголки `::before/::after` — 2.99:1 к индиго, то есть в сотых от порога.
Чинится дизайном, отдельной задачей, приоритет низкий.

**Попытка «усилить пилюлю» в тёмных темах ОТКЛОНЕНА расчётом:** чтобы подложка дала 3:1
к рельсу, нужна альфа ≈0.34, и на такой пилюле акцентный текст падает до 2.37–2.61.
Два требования взаимно исключаются — подложка в тёмной теме носителем состояния быть
не может в принципе.

**Почему это не блокирует мерж:** `audit-contrast.py` в CI НЕ входит (там только
`audit-tokens.py`, `.github/workflows/ci.yml:26`). Обратная сторона — красный аудит никто
не увидит автоматически, поэтому долг записан здесь. **Заводить `audit-contrast.py` в CI
только ПОСЛЕ этой починки**, иначе `main` покраснеет и заблокирует посторонние PR.

Эпик карточки сделки закрыт тремя спринтами (#34 полотно, #35 двухколонник, #37 действия и материалы); разбор решений — `improvements/deals-layout-rationale-2026-08-24.md`.

## Отложено — до сигнала

- **CI-долг**: 36 warnings, `--max-warnings 0` после зачистки; `eslint .` линтит
  `supabase/functions` вне tsconfig. Мина проверена боем в S-AI-OBS-2 (lint 0 на правке
  edge-функции) — риск ниже заявленного. Сигнал: первый инцидент или зачистка по пути.
- **`company_brief` → OpenRouter** — другой инструмент поиска и формат цитат, переписывать
  `stripCiteTags`. Сигнал: повторный отказ Anthropic-канала (18.08 был баланс, не доступ).
- **Контракт `external_*` (ось 4)** — сигнал: у 1С:ДО появляется срок.
- ~~**Роли по воронке (`org_id`, `pipeline_id`, `role`)**~~ — **СНЯТО с отложения 06.09,
  вернулось в очередь спринтом S-DEAL-ROLES-1.** Записанный здесь сигнал возврата —
  «конкретный пример роли, которая зависит от воронки И не является должностью» —
  предъявлен спецификацией W10: `funnel.roles[]` это не роль контакта, а ожидание роли
  в контуре сделки (для IIoT — ЛПР, Основной контакт, Технический эксперт), пустой слот
  которого сам является сигналом. Словарь ролей остаётся универсальным, S-R2-D3 не
  отменяется: `pipeline_expected_roles (org_id, pipeline_id, role, is_required, hint)`
  ссылается на тот же CHECK и следует конвенции «org-специфичные атрибуты глобального
  словаря — отдельной таблицей».
- **S-AI-1 (контекст пресетов из `entity_timeline`) — ОТЛОЖЕНО по данным недели
  наблюдения 22–29.08.** Пресеты не запускают: 3 запуска за неделю, все `company_brief`,
  шесть пресетов из семи — ноль. Гипотеза «мешает узкий контекст» не подтверждена —
  `analytic_note` не вызван ни разу при 3 звонках и 2 транскриптах. Прямое
  противопоказание: расширение контекста растит вход `company_brief`, который
  26.08 уже упал по таймауту на 16,9k. Сигнал возврата: пресет запускают регулярно
  И жалуются на неполноту ответа. Снятие: `claude/observation-ai-2026-08-29.md`.

## Закрыто — август 2026

| PR | Спринт | Суть |
|---|---|---|
| #3 | S-CI-1 | GitHub Actions `checks`: lint + tsc + vitest |
| #4 | S-CI-2 | lint 14→0 ошибок, уход с `next lint` |
| — | ruleset | `main protection`: deletion · non-FF · required `checks` |
| #5 | S-REL-1 | git-cliff, CHANGELOG, тег `v2026.08.1`, `.mailmap`, README |
| #6 | S-MEM-1 | память расщеплена: 166 КБ → 47 КБ + journal 120 КБ |
| #7 | S-EXPORT-1 | `export_org_data`, миграция 126, owner-only |
| #8 | S-AI-OBS-1 | `ai_runs` пишется из capture, миграция 127 |
| #9 | chore/types-127 | реген типов после 127, снят стаб; + STATUS.md, протокол закрытия, status-check |
| — | Netlify | отвязка 21.08 частичная (1 сайт из 3); toriicrm + marshmallow сняты 22.08 |
| #10 | status | STATUS ревизия 3; отвязка Netlify подтверждена верным методом — чистые check-runs PR #10 |
| #11 | S-AI-OBS-2 | исход разбора в `result` (outcome/entity_kind/entity_id), миграция 128; живой смок: created + rejected |
| #13 | docs | закрытие S-AI-OBS-2 (отчёт, память, STATUS 5) + Карта ревизия 5 по постоянной ссылке и в репо |
| #14 | MEMORY.md | индекс памяти CC подрезан 39→13,7 КБ (лимит 24,4); архив побайтово, шапка на состояние 128; STATUS ревизия 7 |
| #15 | Гигиена | 15 scratch-файлов корня → `_analysis/archive-root/`; `.skill` в gitignore |
| #16 | S-MEM-2 | learnings 18 тематических разделов, +45 правил из журнала, «Волны 2» растворены; architecture.md — хроники нет (4%); SKILL.md не резан осознанно |
| #17 | status | ревизия 10 — закрывающие номера #16/#17; хендофф сессии и роадмап-ревизия 22.08 в Claude Project |
| #18 | Карта | ревизия 6: память и гигиена в «сделано», неделя наблюдения — единственный таймер; по постоянной ссылке + копия в репо |
| #19 | status-check | правка реестра, меняющая ТОЛЬКО сам реестр, больше не требует упоминания себя — снята бесконечная регрессия docs-PR |
| #20 | Карта | ревизия 7: горячие точки пересчитаны по оси «размер × вероятность касания» — вывод изменился для двух пунктов из четырёх; STATUS ревизия 13 |
| — | Наблюдение | снятие 29.08 09:00 МСК поставлено запланированной задачей (свежая сессия: воронка исходов → решение по S-AI-1 → отчёт + ревизия STATUS) |
| — | deploy | `telegram-webhook` + `ai-capture` редеплоены; подтверждено живым capture `source='telegram'` 22.08 |
| #21 | status | ревизия 14 — PR #20 в реестр, отметка о закрытии сессии 22.08 |
| #22 | S-TR-CREATE-1 | транскрипты v2: мастер создания (звонок/встреча под капотом, миграций нет), колонка «Сделка» + бейдж прогонов, группировка фрагментов; `ui/SegmentedControl` — три копии сегментированного контрола сведены в одну, форма пилюли. Гейт 23.08: ролевой смоук под manager закрыт фактом, смок-данные вычищены |
| #23 | status | закрыт без мержа: ревизия 15 поглощена ревизией 16 (#24); реестр сведён к одной линии |
| #24 | S-TR-VTT-1 | `stripSubtitleMarkup` для вкладки «Файл» (WEBVTT/SRT), 12 тестов; дефект первой редакции — блок `NOTE` до пустой строки — пойман смоуком владельца. Vitest прогнан владельцем перед мержем |
| #25 | S-HEALTH-V2-1 | здоровье сделки как сигнальная панель: пять именованных сигналов, вердикт new/ok/attention/rotting, пороги в `organizations.settings`, CTA на сигналах. Удалены мёртвый фактор `last_contact_date` (колонки нет в БД) и инверсия дедлайна; «шаг без даты» — warn, не bad (расходился с `DealHealthDot`). Quick wins визуального аудита F-01/03/05/08. Миграций нет. По живой БД: 8 «Киснет» / 2 «Новых» вместо «красное у всех» |
| #26 | docs | закрытие S-HEALTH-V2-1: урок в журнал, четыре правила в learnings, STATUS ревизия 17 |
| #27 | S-STAGE-STORY-1 | подсказка стадии (`stage_guidance` в настройках org, правка на месте у owner) и вкладка «История»: сегменты стадий из `stage_transitions`, суммарное время при повторных заходах, возвраты, переносы дедлайна из аудита 087. Миграций нет. Попутно снят дефект темы Fuji: позиционный `nth-child(5)` прятал пятую вкладку любой полосы |
| #28 | docs | закрытие S-STAGE-STORY-1: урок в журнал, четыре правила в learnings, STATUS ревизия 18 |
| #29 | S-QUEUE-1 | очередь дня: «гниющие сделки» разделены на «Просрочен шаг» и «Без плана» (тот же `getDealHealth`, что на карточке); личный snooze строки — **миграция 129 `queue_snoozes` применена гейтом, ledger `20260823182046`**, RLS org-first + `created_by = auth.uid()`, ролевой смок 7/7. Типы регенерированы, стаб снят и проверен на живость. Вкладок-срезов не вводили: у TodayView семь специализированных секций и сквозная j/k-навигация — три вкладки поверх них были бы шагом назад |
| #30 | docs | закрытие S-QUEUE-1: урок в журнал, три правила в learnings, STATUS ревизия 19 |
| #31 | S-TOKENS-CONTRACT-1 | долг «Контракт токенов CSS» ЗАКРЫТ. Зачистки не потребовалось: альфа на `-l`, Tailwind-палитра, `hover:elevation`, ручной лист — 0 (цифры STATUS от 22.08 устарели, закрыли соседние волны). `scripts/audit-tokens.py` — семь правил + реестр исключений с обоснованиями, шаг `Token contract` в CI; раздел «Контракт токенов» в `theme-system.md`. Свотчи тем сведены в `src/lib/constants/themes.ts` — frost/aurora/tidal показывались в шапке и настройках разными цветами. `globals.css` не резан, important-правила не сняты (решения из STATUS в силе) |
| #32 | docs | закрытие S-TOKENS-CONTRACT-1: урок в журнал, три правила в learnings, STATUS ревизия 20 |
| #34 | S-DEAL-CANVAS-1 | полотно детальных страниц: токен `--content-max` (90rem) и класс `.content-shell` на пяти карточках — deals/projects/companies/contacts/leads; доски, таблицы и Гант остались full-bleed. Снято «Пройдено N%» (дубль закрашенной рельсы, остаток F-03); пустая подсказка стадии у owner — тихая строка вместо полосы-рамки; `max-w-[80ch]` на теле события ленты. `InlineEdit` получил `startEditing` и `onCancel` (необязательные, прежние call-site не менялись). Миграций нет. Разбор и мокап — тем же PR |
| #35 | S-DEAL-RAIL-1 | двухколонник карточки сделки `работа │ контекст`: сетка `minmax(0,1fr) 320px`, рельса sticky справа. `RailCard`/`RailRow` подняты из `CompanySidebar` в `shared` — одна рельса на два экрана, обе на `<aside>` (прежний запрет снят S-FIX-CO360-1, проверено по всем `.t-* aside`). Инфо-грид → property-list «Сводка»; бейдж полноты и «Создана» из шапки туда же; `DealFocusPanel` расщеплён на `DealNextStep` + рельсу и оставлен только для peek; `DealHeader`, `CompletenessBadge`, `ProjectMaterials` вынесены — `ProjectDetail` 949 → 539. `SIGNAL_ANCHORS.deadline` → `deal-summary`. `StageRail`: `shrink-0` на группах — карта скроллится, а не сжимается. Вертикаль до вкладок 580 → **426** (норма ≤400 — добирается в спринте C выносом «Материалов»). Миграций нет |
| #36 | docs | закрытие спринтов A и B: журнал 2026-08-24 + пять правил в `learnings` — проп «открыться сразу» через `useState` живёт только размонтированием; комментарий-запрет переживает свою причину (`.t-* aside` сужены S-FIX-CO360-1); подъём приватного примитива в общий не меняет дефолт поведения; мокап собирается на дефолтном состоянии компонента, не на присланном скриншоте; ветка от невлитой ветки — ребейз до ревью. Скилл раскатан, пакет загружен в аккаунт |
| #37 | S-DEAL-CTX-1 | действия `+ Задача / Звонок / Встреча` на полосу вкладок — видны на всех вкладках, `flex-wrap` на обоих уровнях (при колонке 556px полоса раскладывается в две строки, ничего не режется). «Материалы» — `DealMaterialsCard` счётчиками в рельсе, раскрытие в модалке на `shared/Modal`; `ProjectMaterials` удалён, аккордеон и его горизонтальный уровень ушли из потока. Пустые состояния файлов и видео на `ui/EmptyState` с ценой пустоты вместо «пусто» (F-04). **Вертикаль до вкладок 426 → 372 при норме ≤400 — норма выполнена впервые.** Цена R-08 названа заранее и подтверждена смоком: ровно два запроса на карточку, `count: 'exact'` не заводился. Миграций нет |
| #38 | docs | закрытие эпика карточки сделки: журнал S-DEAL-CTX-1 (700 → 580 → 426 → 372) + два правила — посылку «данных для смока нет» проверять запросом, а не глазами в UI; числовой порог плотности переносить вместе с контекстом (полотно ≠ модалка) |
| — | Наблюдение | **Неделя наблюдения AI (22.08 → 29.08) ЗАКРЫТА** — снятие `claude/observation-ai-2026-08-29.md`. Capture: 9 прогонов, 5 дошли до сущности (56%), `unclear` 0%, `matched_existing` живыми данными не подтверждён. Все 5 компаний недели заведены через capture, вручную — ноль. Вход 11 100 токенов — константа подтверждена (разброс 0,18%), $0,106/неделя на haiku 4.5: вопрос оптимизации входа закрыт, не трогать. Пресеты: 3 запуска, один пресет из семи, 1 из 3 — таймаут. **Решение: S-AI-1 → Отложено** (см. раздел выше). Единственный таймер проекта снят — очередь снова пуста. ⚠️ Снятие делала scheduled-сессия без моста к машине: патч STATUS в §7 снятия написан на ревизию 10 и применять его дословно нельзя — учтён здесь по смыслу |
| #39 | Гейт | тесты в DoD: секция `## ТЕСТЫ` в формате спринта и два пункта чеклиста (кейсы описаны поведением; время и рандом — аргументом функции, не `Date.now()` внутри). Маршрутизация линз ревью по диффу — базовая всегда, `security-privacy-reviewer` на миграции/RLS/роли/Storage, `crm-design-auditor` + `a11y-auditor` на новый экран/темы, `api-architect` на Server Actions и внешние API, `supabase-patterns` на хуки/realtime; линза без результата = пропуск, лимит ≤8 находок с цитатой из диффа. Cold review на спринтах с миграцией/RLS/ролями/деньгами: дифф + acceptance criteria в чистую сессию без спринт-файла. Красная линия «гейт не дублирует CI». Правки только в `crm-architect/SKILL.md`; парная правка `sprint-prompt-builder` (v2.1) живёт вне репо и в этот PR не входит |
| #41 | fix | `getDealHealth` принимает `now` — вердикт больше не читал часы сам. CI ветки гейта краснел с 25.08 при диффе из одних `.md`: пять падений в `deal-signals.test.ts`, везде «rotting». `getDealSignals` принимала `now` и передавала его в `getNextActionOverdueDays`, но `nextStepSignal` звала `getDealHealth(project)`, а та делала `new Date()` внутри — кейсы с шагом «завтра относительно NOW=23.08» протухли, как только реальная дата ушла за 24.08. `now` параметром с дефолтом: 12 call-site'ов в UI и `project-filters` не меняются; арифметика сравнения не тронута (решение S-TAILS-1 в силе). Тесты: 5 кейсов в `deal-health.test.ts` — тот же вход с `now` и `now + 7 дней` обязан давать разные вердикты. Правило в learnings: «время аргументом» относится ко ВСЕЙ цепочке вызова, а не к публичной функции. Миграций нет |
| #40 | Скиллы | Три скилла из obra/superpowers файлами — `debug-forensics`, `worktree-isolation`, `sdd-controller`; плагин целиком отклонён (коллизия двух обязательных процессов, TDD-ядро не по стеку) — разбор `claude/eval-superpowers-2026-08-29.md`. При переносе: падающий тест в фазе 4 заменён на воспроизведение (SQL-смок statement'ами, ролевой прогон под JWT, браузерный сценарий), `npm run build` убран из baseline, стоп-условий у `sdd-controller` шесть вместо четырёх, pre-flight scan проверяет проектную специфику. `sprint-prompt-builder` переехал в репо: он парный к контракту гейта и вне git расходился молча. `skill-deploy.sh`/`skill-verify.sh` параметризованы именем скилла (CORE по скиллу, без аргумента — `crm-architect`, старые вызовы не меняются). Два правила в learnings: git из Cowork звать МОЖНО после запроса права на удаление (прежний запрет помечен отменённым), и `skill-deploy` из Cowork раскатывает в `$HOME` сессионной VM, а не в `~/.claude/skills` — «раскатано» оттуда не значит раскатано. Миграций нет |
| #42 | fix | `skill-deploy.sh` падал на «CORE[@]: unbound variable» у трёх новых скиллов: их `CORE` пуст, а `/bin/bash` в macOS — 3.2, где развёртывание пустого массива под `set -u` запрещено. Параметризация проверялась из Cowork на bash 5.1, где это легально, — дефект дошёл до машины владельца. `CORE` и `REFS` развёртываются через `${ARR[@]+"${ARR[@]}"}`; проверено раскаткой всех трёх на macOS. Правило в learnings: проверка скрипта в мосте НЕ равна проверке на машине владельца — там другой bash и другой `$HOME`. Правок кода приложения нет |
| #43 | Контракт | Долг закрыт: контракт гейта предписывал звать `.grok/skills/review-diff/scripts/cold-review.sh`, а вся `.grok/` лежала в `.gitignore` — шаг невыполним для любого исполнителя, кроме владельца, и это не ловят ни CI, ни `status-check`. Игнор сужен с `.grok/` до `.grok/*` + `!.grok/skills/`: скиллы `review-diff` и `review-sprint` с их скриптами (474 строки, секретов и машинных путей нет, grok CLI ищется через `PATH`/`GROK_BIN`) версионируются; состояние остаётся локальным — вход cold-review содержит дифф рабочей ветки, watcher держит pid и логи. Путь вызова не менялся намеренно: перенос в корень по конвенции `crm-architect/` сломал бы контракт и вызовы CLI ради косметики. В контракт добавлено условие «запуск требует grok CLI на машине, без него шаг выполняет владелец». Правило в learnings: контракт в репо не смеет ссылаться на путь из `.gitignore` — игнорировать состояние, а не папку с активом. Правок кода приложения нет |
| #44 | Скиллы | Проводка CRM-ветки скиллов по ревизии графа ссылок (`crm-ui-designer` — 0 входящих, `debug-forensics`/`sdd-controller` — 0): в `crm-architect` Feature Design получил стадию экрана (DB → Types → Screen по апрувленному мокапу `crm-ui-designer` → Hook → Component), редизайн через `crm-design-auditor`, тексты состояний — `copy-calm`; Bug Diagnosis подчинён `debug-forensics` («ни одного фикса до Фазы 1»); дубль скелета спринт-промпта убран — формат только в `sprint-prompt-builder`; секция «Роли и маршрутизация — цикл фичи»; правило тем (theme-system.md — истина, `theme-factory` не про нас). `sprint-prompt-builder`: worktree-isolation/sdd-controller в цикле, «экран — по мокапу» в правилах передачи и Missing Context. Четыре процессных скилла получили футер «Источник и раскатка»; протокол закрытия шаг 6 — для любого скилла из репо. Зафиксировано: branch protection на `main` включён (прямой пуш отклонён GH013), `status-check` в CI не стоит. Аккаунтные пакеты шести скиллов вне репо (crm-ui-designer, crm-design-auditor, design-system-architect, design-instructor, qa-engineer, forms-architect) обновлены отдельно. Правок кода приложения нет |
| #45 | S-LIME-TOKENS-1 | **восьмая тема `t-lime`, она же новый `DEFAULT_THEME`.** Лайм `#C9F25A` — цвет ЗАЛИВКИ, не текста (на белом 1.29:1): пара `--accent`/`--accent-text #336809`, `--on-accent #14210A`, плюс два правила в `globals.css` — на 82 места с `bg-accent text-white` и на 321 с `text-accent` (утилита резолвится в `var(--accent)`, скрипт этого не видит — иконка в плитке компании давала **1.10:1**). Семантика затемнена против minimal: у темы цветные подложки зон, там значения minimal дают 4.13–4.34:1. Токены `--zone-*` для всех тем через `color-mix`, база — `--popover`, НЕ `--surface` (в glass-темах он полупрозрачен → зоны с alpha 0.49). Примитив `.entity-tile`. Рельс нава — единое полотно: погашены четыре линии, три из них инлайн-стили. `resolvePersistedTheme` вынесен из `merge()` и покрыт тестом (10 кейсов, ключевой — `t-minimal` переживает смену дефолта). `t-minimal` не тронута, миграции persisted нет. `audit-contrast.py`: 0 FAIL на всех восьми, у lime 38 пар — столько же, сколько у minimal. Миграций нет. Решения — `claude/decisions-lime-theme-2026-09-05.md` |
| #46 | chore | артефакты за 29.08–05.09 в `_analysis/`: спринты S-LIME-TOKENS-1 и S-FORMAT-1, заготовка `fix-AUDIT-NAV-CONTRAST`, cold-review PR #41, лаборатория тем |
| #47 | docs | закрытие S-LIME-TOKENS-1: `theme-system.md` (восемь тем, дефолт lime, токены зон, `.entity-tile`), журнал, пять правил в learnings, STATUS ревизия 31 |
| #48 | fix | **фон lime исправлен дважды-ошибочно и наконец верно.** `#EDE9E1` → `#F2EFE9` → `#FAF8F5`: обе первые итерации опирались на правило minimal «фон темнее белого, чтобы карточки отделялись», а в lime карточку отделяют тень и подложки зон. Следствия: `--surface2` перевёрнут в утопленный слой, `--zone-base` считается от `--bg` + 2% `--text` вместо `--popover` (непрозрачная поверхность при белом фоне давала базу светлее фона). Зоны сошлись с макетом в 3-4 единицы на канал |
| #49 | fix | `audit-contrast.py` считает пару активного пункта нава у **всех восьми** тем вместо двух. Каскад разрешается по факту: рельс по карте `NAV_RAIL`, пилюля из `--sidebar-indicator` только если непрозрачен (иначе layered `--accent-l`), текст из `--sidebar-active-text` — компонент читает его утилитой из `@layer utilities` и она бьёт layered `.nav-active`; хардкод washi/fuji оставлен именованным исключением. Понижение пары «пилюля / рельс» до info-only **вычисляется**, а не прописано списком: если актив и неактив по светлоте неразличимы (< 1.5), подложка становится единственным носителем и порог 3:1 применяется всерьёз. Попутно `--sidebar-active-text` в `.t-frost`/`.t-aurora` уведён на `--accent-text`: 4.00 → 5.85 и 3.82 → 6.77. Открыт долг по трём тёмным темам (см. «Очередь») |
| #50 | docs | карта носителей состояния активного пункта нава по восьми темам в `theme-system.md`; долг переформулирован — три FAIL у тёмных тем ЛОЖНЫЕ (у них работает `border-left` 3px акцентом: 4.96 / 4.59 / 9.97 к рельсу), чинить надо скрипт, а не темы. Расчётом отклонён вариант «усилить пилюлю»: 3:1 к рельсу требует альфы ≈0.34, на такой пилюле акцентный текст падает до 2.37 — в тёмной теме подложка носителем быть не может |
| #51 | S-FORMAT-1 | форматтеры карточки сделки: `formatBudget` → «2,8 млн ₽» (латинская M и точка ушли), `parseBudgetInput` понимает млн/тыс/m/k и больше не глотает точку (`2.8` читалось как 28); единый `formatContactName`/`Short` вместо двух правил; `formatDateNumeric` — один формат даты в рельсе + `tabular-nums`; «Здоровье» в рельсе показывает только помехи, норма свёрнута в «N в норме»; пустая подсказка стадии проявляется по наведению (`group/cockpit`), а не занимает строку. Тесты: format-budget 13 кейсов с round-trip-инвариантом, contact-name 8+8. **F-01 на гейте не сработал и был доломан там же:** в базе пустая фамилия записана дефисом (5 контактов из К7), «- Н.» осталось на экране — заведён набор заполнителей с ТОЧНЫМ сравнением (в базе есть настоящие «Цой», «Гук») |
| #52 | docs | закрытие S-FORMAT-1: журнал, четыре правила в learnings, STATUS ревизия 33. **Свой номер в реестр не внёс** — краснел на `status-check` до ревизии 34 |
| #53 | S-DEAL-ZONES-1A | каркас: карточка сделки `type='client'` разошлась на три зоны — «Работа» (кокпит, шаг, вкладки) слева, «Риски» и «Контекст» справа в колонке 356px, шаг между зонами 20px. `DealContextRail` остался прежним путём для внедрения и internal; общие карточки вынесены в `DealRisksZone`/`DealContextZone`, двух копий нет. Токены `--zone-*` из S-LIME-TOKENS-1 получили первых потребителей. Миграций нет |
| #54 | S-DEAL-ZONES-1B | кольцо здоровья в зоне «Риски» + «перенесён N раз» у шага. Кольцо — счёт сигналов, НЕ балл: взвешенный score из макета (100 минус веса 30/12/10) отклонён — весов у сигналов нет, а выдуманные вернули бы непрозрачное число, ради ухода от которого `calculateDealHealth` снят в S-HEALTH-V2-1. Счётчик переносов приезжает из того же queryFn, что уже считал переносы дедлайна (аудит полей 087): ключ `deadline-moves` → `field-moves` вместе с единственной инвалидацией в `useUpdateProject.onSettled` — realtime у хука нет намеренно. Счёт строгий: только сдвиг вперёд, только у текущего шага (границы — назначение даты и «Шаг сделан»), порог показа ≥2. Миграция `postpone_count` не понадобилась. Миграций нет |
| #56 | fix | **отменён #57, влился по недосмотру.** Промежуточная правка кольца: сегменты над списком вместо «слева», палитровая ступень цвета вместо `*-text`, 96px/обводка 9/зазор 4°. Оказался открыт с ранее взведённым `gh pr merge --auto` и влился сам после мержа соседнего PR, откатив уже принятый виджет. Из содержательного пережило только «кольцо над списком» и возврат палитровой ступени — оба перенесены в #57 |
| #57 | fix | **финальное кольцо по макету: одна дуга, не сегменты.** Серая дорожка на полный круг, поверх одна дуга цветом худшего состояния, доля дуги — помехи от всех применимых сигналов. Сегментация была ошибкой чтения макета: там она живёт в горизонтальной пятиполосной шкале, а Р3 убрал её как дубль списка — перенеся сегменты в кольцо, вернули убранное. Полоса вернулась отдельно и стала единственным носителем посигнального разреза, с подписью словами (читается без различения цвета). Кликов по дуге нет — дуга ведёт не к одному сигналу, переход делает CTA в строке. Вердикт и заголовок «Здоровье» переехали к кольцу, `DealNextStep` лишился пропа `signals`. Плюс подложка критичного сигнала и иерархия строки. Миграций нет |
| #55 | fix | тон зоны «Работа» — свой токен `--zone-work-tint`, а не производная `--accent`. Формула #53 молча считала акцент свободным тоном; это верно только в лайме. ΔE (CIE76, порог различимости плашек ≈5): tidal 1.8, washi 2.8, aura 3.3, minimal 4.9 — четыре темы из восьми сливались с соседней плашкой. Четвёрка уводит тон в `--purple` (единственный палитровый тон без семантического алиаса), худший ΔE становится 7.2 / 5.0 / 8.7 / 8.8. `t-fuji` НЕ в списке: там акцент и `--blue` — одно значение, purple опускает пару с 5.0 до 4.1. Eyebrow зоны идёт тем же токеном `--zone-work-ink` и в четвёрке уходит на `--text-dim` — иначе в washi «РАБОТА» набрана красным и читается как ошибка. Таблица и правило — `theme-system.md`. Миграций нет |
| #59 | docs | закрытие эпика зон карточки сделки: журнал, две линии + процессное правило в learnings, таблица ΔE и правило про `--accent` в `theme-system.md`, шаг про `.env.local` в `worktree-isolation`, STATUS ревизия 34. Свой номер внесён вторым коммитом после `gh pr create` — по правилу, добавленному этим же PR |
| #60 | docs | **хвост ревизии 34, ревизия НЕ инкрементирована** — добор в её рамках, не новое закрытие. Правило про PR на сквош-мерженной ветке: сквош оставляет ветку живой и внешне «непримёрженной», а её файлы застывают на состоянии до последующих PR — мерж такого дубля откатывает всё, что легло в `main` после (#56 откатил кольцо, #58 откатил бы zone-work-tint). Проверка `gh pr list --state open` после каждого сквош-мержа — в `SKILL.md` к красной линии «мерж и пуш руками», разбор — буллетом под правилом про `--auto` в learnings. Найдено на самой ревизии 34: пока текст писался, #59 смержился сквошем и `chore/skill-rev35` стала ровно такой веткой |
| #61 | fix | **пакет для аккаунта привязан к репозиторию и наконец проверяется. Ревизия не трогалась** — дефект инструмента, не закрытие спринта. `skill-deploy` собирал `<skill>.skill` относительно `$PWD`: раскатка из worktree клала пакет в корень worktree, локальная копия обновлялась верно (её путь от `$HOME`), `skill-verify` был зелён — а в основном чекауте пакет остался от 05.09 при ревизии 34 в источнике. Путь привязан к корню ОСНОВНОГО чекаута — родителю общего git-dir; `--show-toplevel` не годится, в linked worktree он отдаёт корень worktree, то есть ровно проблемный путь. `skill-verify` распаковывает пакет и диффает его с ИСТОЧНИКОМ (не с локальной копией — производные расходятся независимо): расхождение, отсутствие и битый архив дают ненулевой выход, пакет старше источника по mtime — предупреждение (git переписывает mtime при checkout/rebase, жёсткая проверка краснела бы после каждой смены ветки). Прогнано на четырёх состояниях. Правок кода приложения нет |
| #62 | docs | **STATUS ревизия 35.** Пять спринт- и ревью-файлов эпика зон карточки сделки заведены под git — жили untracked в `_analysis/` и в ревью не попадали. S-DEAL-DATA-1 снят из очереди как закрытый чужими спринтами, с разбором по каждому пункту (`postpone_count` → #54 и аудит 087; консьюмеров `stage_dwell_defaults` два, а не ноль; `events.effects` = `payload.changes`; «готовность N/N» = `ProjectStageCockpit`). Роли по воронке — в «Отложено», не в «Закрыто»: противоречат S-R2-D3. Корень: очередь бралась из документа решений, составленного по review макета, а не кода — зеркальное правило к «макет старше решений» дописано в learnings |
| #63 | S-DEAL-CHZ-1 | **STATUS ревизия 36.** Блок «Честный Знак» в зоне «Контекст» карточки сделки: `useCompanyChz` отдельным запросом (`PROJECT_SELECT` не тронут — джойн тянется в каждом списке сделок), `CHZ_SNAPSHOT_DATE`/`CHZ_SNAPSHOT_SOURCES` вместо даты в комментарии, синхронно в зеркале Deno. Миграций нет. Три находки разведки: `resolveChzProfile` схлопывает `NULL` и `[]` в один `source='none'` — различие «не спросили» / «спросили, ответ отрицательный» восстановлено в UI по сырой колонке, домен не тронут; `chz-profile.test.ts` уже существовал с S-LEAD-CARRY-1 и покрывал 6 кейсов Т2 из 7; стаб `CompaniesChzStub` снят регеном после 123 — предупреждение спринта устарело. Сверх спринта: дата снапшота была вторым источником истины в `CompanySidebar` и расходилась форматом; заведён `formatCalendarDate` — `new Date('YYYY-MM-DD')` даёт UTC-полночь и при отрицательном смещении зоны уезжает на сутки назад. **Латентный дефект найден по ходу:** `DealSummaryCard` печатал так дедлайн сделки, `try/catch` не защищал (исключения нет, дата просто неверная). Хвост: визуальная приёмка в восьми темах владельцем НЕ делалась |
| #65 | docs | **STATUS ревизия 37 — эпик «Сделка v2» заведён целиком.** Спецификация макета вытащена из бандла в самостоятельный HTML и заведена под git вместе с картой «спека против кода» и четырьмя написанными спринтами (`deal-v2-spec.html`, `deal-v2-gap-2026-09-06.md`, ROLES-1 / PULSE-1 / SUMMARY-1 / CHZ-2) — до этого спека читалась вне репозитория и в ревью не попадала. Очередь эпика переехала из `decisions-lime-theme` в STATUS: семь спринтов, у первых четырёх путь к файлу, спринты 5–7 не написаны намеренно — до развилки Р-A это два разных спринта под одним именем. Три решения владельца вынесены отдельной строкой: Р-A (вкладки против стопки), Р-Б (score — отказ в силе), Р-В (ЧЗ на не-IIoT); четвёртая развилка карты (два определения полноты) решается внутри файла SUMMARY-1, в STATUS не поднята. **S-DEAL-DATA-1 закрыт:** три пункта чужими спринтами, четвёртый ожил как S-DEAL-ROLES-1 и вернулся из «Отложено» — сигнал возврата, записанный там дословно, предъявлен спецификацией W10 (ожидание роли в контуре, а не словарь должностей; S-R2-D3 в силе). Единственная миграция эпика — **130** `pipeline_expected_roles`, в спринте 1. В learnings третий случай к «макет старше решений»: очередь эпика из документа решений ошибается в ОБЕ стороны — лишняя работа по закрытым пунктам и потерянная по снятому зря, а проверка «сделано ли уже» ловит только первую половину. Правок кода нет, миграций в PR нет. Заменяет #64 (подмножество) |
| #66 | docs | **STATUS ревизия 38 — три развилки эпика «Сделка v2» закрыты, очередь выросла до восьми спринтов.** Р-A → гибрид (стопка Кокпит → Шаг → Доска задач → Орг. блок, под ними вкладки Активность · Гант · История · Чат; вкладка «КП» упраздняется), Р-Б → score не возвращаем (третья формула запрещена `deal-signals.ts`), Р-В → ЧЗ не скрываем, правок не требуется (`DealChzCard` и так отдаёт `null` на пустом профиле). Из решения Р-A выделен отдельный спринт **S-DEAL-LAYOUT-1**: перестройка раскладки делается один раз и ДО трёх виджетов, которые в неё встают, иначе конфликт в `ProjectDetail.tsx` и неразличимый дифф. Написаны последние четыре спринта (LAYOUT-1 / DEADLINES-1 / ORG-1 / EVENT-1) — очередь эпика впервые вся на бумаге. Независимы 1, 2, 4, 5; спринт 3 ждёт ROLES-1 и влитого CHZ-1 (#63), спринты 6–8 — пятого. Шапка STATUS исправлена: в ревизии 37 она осталась на `8b85991` при `main` = `08bfcaa`. Заодно починена строка реестра — в ревизии 37 запись #65 приклеилась к записи #63 одной строкой (вставка съела перевод строки). Правок кода нет, миграций нет |
| #67 | docs | **правила worktree в скилл `worktree-isolation`, ревизия STATUS не трогалась** — правка инструмента, не закрытие спринта. Шаг 0.4: worktree заводится под ветку С КОДОМ, документные ветки (STATUS, learnings, journal, `_analysis/`) ведутся в основном чекауте — worktree держит вторую копию `_analysis/` и она расходится молча, ни `git status`, ни `tsc`, ни lint этого не видят. Шаг 1b: имя папки worktree обязано совпадать с веткой, папку не переиспользовать (`feat+deal-zones-1a` прожила три ветки подряд). Правок кода приложения нет, миграций нет |
| #68 | docs | **дубль #67 с ПУСТЫМ диффом** (`git diff 0d1768f 7765131` — ноль файлов): тот же заголовок, то же тело, дерево не изменилось. Смержен вторым. В реестр внесён не ради содержания, а чтобы `status-check.sh` не краснел на номер, за которым ничего не стоит, — и чтобы след двойного мержа остался виден |
| #69 | docs | вход спринта S-DEAL-ROLES-1 переведён на актуальный `main`: в файле спринта стояла база `a3d182d`, к моменту запуска `main` ушёл на `7765131`. Одна строка в `_analysis/sprint-S-DEAL-ROLES-1.md`. Правок кода нет, миграций нет |
| #70 | S-DEAL-ROLES-1 | **Влит; миграция 130 применена гейтом 07.09 (`20260907073548`).** W10: карта стейкхолдеров стала слотами ожидаемых ролей воронки. Миграция **130** `pipeline_expected_roles` (org-таблица, как `stage_requirements` 027, а не колонка в глобальном `pipelines`; RLS шире у SELECT — все члены org, уже у CUD — owner/admin, потому что состав контура это настройка организации, а не правка сделки) **применена гейтом 07.09, разбор гейта — строкой ниже**. Слот primary — виртуальный, `projects.contact_id` вторым источником истины не дублируется (урок `companies.phone` ↔ `phones[]`); счётчик «N из M» считает СЛОТЫ, не головы. Сигнал `single_threaded` перешёл с количества стейкхолдеров на покрытие обязательных ролей; severity осталась `warn` — `bad` поднял бы вердикт до `rotting` и покрасил зону «Риски» у любой рабочей сделки с пустым слотом ЛПР. Воронка без ожиданий сохраняет прежний плоский список и прежний сигнал — обратная совместимость delivery-воронок. **Словарь ролей расширен седьмым значением `influencer` (ЛВР)** той же миграцией: точек синхронизации оказалось ТРИ CHECK'а, не два — про `leads_decision_role_check` (123, применён) забыть было нельзя, селект «Роль контакта» в `LeadModal` строится из `STAKEHOLDER_ROLE_ORDER`, и без правки он ловил бы новое значение 23514 прямо в форме. Cold review (86/100, MERGE) дал F1: пустой слот primary вёл в добавление участника, которым не закрывается, — переведён на `onEdit` сводки, третьего пути правки контакта не заведено |
| #71 | S-DEAL-PULSE-1 | **STATUS ревизия 40.** W8, пульс активности сделки за 30 дней — единственный виджет спеки, которого в коде не было вовсе. `deal-pulse.ts`: `buildDealPulse` (30 календарных дней без пропусков, ось дня — `mskDateKey`, НЕ `day-windows.ts`; 15 точек спарклайна — спека называла 16, офф-бай-ван 30/2; разрыв тишины считается с учётом хвоста, время аргументом) + `silenceWithin(days, n)` отдельной функцией для тепловой полосы (14 из 30 дней) — общий агрегат домена не годится для подписи над срезом, разные N разъехались бы. `use-deal-pulse.ts`: своя колонка без `limit` (`useActivityLog` с `limit(50)` показал бы неправду на активной сделке), `neq` на техсобытие перехода стадии, без realtime. `DealPulseCard` в зоне «Риски» после «Закреплено»: иконка `TrendingUp` (`Activity` занята «Здоровьем»), цвета — `--green`/`color-mix`, не `--lime` (в токенах нет) и не `--accent` (в `t-washi` это `--red`). Скрытие только когда `total=0` И сделка создана сегодня — аналогия с `PinnedNoteCard` (тот пустым НЕ скрывается) не работала. Спринт пришёл уже с зашитыми фиксами собственного ревью (82/100 NO-GO): блокер B1 (`mskDateKey`) и W1 (15 точек) были прописаны в тексте до старта работы. Тесты — 11 кейсов, `tsc`/`lint`/`vitest` (1912 тестов)/`build` чисты. Миграций нет — закрытие сразу, не дожидаясь гейта apply, в отличие от ROLES-1. Свой номер внесён вторым коммитом после `gh pr create`. |

| #72 | Гейт | **apply 130 — STATUS ревизия 41.** `pipeline_expected_roles` применена 2026-09-07 (`20260907073548`); словарь ролей расширен `influencer` во ВСЕХ трёх CHECK'ах (`deal_stakeholders_role_chk`, `leads_decision_role_check`, `pipeline_expected_roles_role_chk`). Сид сел на «IIoT Продажи» и «ERP Продажи» и НИ на одну delivery-воронку — обратная совместимость подтверждена запросом, а не рассуждением. Advisors: ни одного нового WARN (initplan-обёртка `( select … )` сработала), по таблице только INFO — unindexed FK на `created_by`/`pipeline_id` и `unused_index`; второй индекс под FK НЕ заводится намеренно: строк единицы, удаление воронки — операция раз в год, плата за запись перевесила бы выгоду. Ролевые смоки пройдены под owner, admin, manager, viewer и не-членом org: читают все члены org, пишут только owner и admin, посторонний видит ноль строк. **Конверсия лида с `decision_role = 'influencer'` проверена сквозным вызовом `convert_lead`, а не сравнением определений CHECK'ов** — роль доехала до `deal_stakeholders.role` без 23514. Смоки прогнаны в транзакциях с откатом, следов в данных нет (проверено отдельным запросом). Хвост: реген типов и снятие стаба `PipelineExpectedRolesStub` — за владельцем, у моста нет сети для `npx` |

| #73 | docs | **дыра в реестре, закрыта задним числом 08.09 — `status-check.sh` краснел на неё с 07.09.** `worktree-isolation`, Шаг 4: уборка worktree и ветки после мержа PR (`unlock` → `remove --force` → `prune`; ветка `-D`, потому что сквош-мерж не делает её коммит предком main и `--merged` её не покажет; список к `push --delete` строить ПОСЛЕ `fetch --prune`). Не попал в закрытия ревизий 42 и 43: писался между двумя спринтами и в оба реестра не вошёл. Правок кода нет |
| #74 | docs | спринт-файл S-DOCS-PR-1 в репозиторий: вход переведён на актуальный `main`, добавлено исключение для ЧИСТО комментарной правки `.sql` под `supabase/migrations/` (без него скрипт отверг бы гейтовый PR #72 — шапки 129/130 «НЕ применена» → `applied`), шаг 5б с подстановкой номера PR по литералу `\| #PR \|` вместо второго ручного коммита, отказ на висящем `index.lock`. Правок кода нет |
| #75 | S-DOCS-PR-1 | **`scripts/docs-pr.sh` — документная правка одной командой. STATUS ревизия 42.** Ветка от свежего `origin/main` → коммит (`-F -`, не `-m`: zsh глотает `!` в многострочном сообщении) → PR с ЯВНОЙ базой → подстановка номера → сверка `baseRefName` (молча продолжать нельзя: так ушёл в никуда #67) → авто-мерж → возврат. Семь отказов ДО первого действия. Гейт дал три находки, все исправлены: **тихая потеря `.sql`** (автоопределение их не подхватывает, а шаг 4 пропускает — PR уехал бы без миграций молча, теперь отказ с именами файлов), разбор `PR_NUM` из последней строки вывода `gh` с валидацией, отказ на опечатке в явном списке вместо молчаливого выпадения. Второй заход гейта нашёл главное: **`--auto` при `mergeStateStatus=BEHIND` не сработает НИКОГДА**, а скрипт печатал «смержится сам» — найдено на живом #74, висевшем час при зелёном CI. Теперь шаг 8 читает `state` и `mergeStateStatus` одним вызовом, один раз зовёт `gh pr update-branch` и не врёт в трёх исходах. `DOCS_PR_NO_AUTOMERGE=1` — позитивный путь проверяется без гонки со сторожем и без ставки на `main`. Юнит-тестов нет: bash, проверен восемью живыми сценариями отказа плюс позитивным прогоном |
| #78 | docs | закрытие S-DOCS-PR-1: журнал и learnings за 07.09 (BEHIND, стоимость стенда, гонка со сторожем), долг «X из 5» в кольце здоровья в очередь. **Первый боевой прогон `docs-pr.sh`** — этой записью он и проверяется: номер выше подставлен скриптом |

| #79 | docs | вход S-DEAL-LAYOUT-1 переведён с `08bfcaa` на `cfa1970` (десять PR разницы), номера строк разведки сверены с текущим `ProjectDetail.tsx`, добавлена задача 6 — подпись кольца здоровья из очереди долгов. Правок кода нет |
| #80 | S-DEAL-LAYOUT-1 | **Р-A: зона «Работа» — стопка вместо шести вкладок. STATUS ревизия 43.** Доска задач и орг. блок переехали из вкладок в `CollapsibleSection` (общий примитив, состояние per-виджет per-сделка в `localStorage`, чтение и запись в try/catch); вкладка «КП» упразднена, `?tab=quotes` разворачивает орг. блок, скроллит к нему и чистит параметр — деплинк читается СИНХРОННО на первом рендере, эффектом была бы гонка с чтением хранилища. Компоновка `delivery`/`internal` не тронута: у них зон нет вовсе, доска остаётся вкладкой. **Задача 6:** центр кольца говорит «N помех» словом вместо «N из 5» — форма досталась от отклонённого score (Р-Б), где числитель означал ЗДОРОВЬЕ, и после замены на счёт сигналов знак поменялся на противоположный; расчёт и вердикт worst-wins не тронуты. Гейт дал две находки: **содержимое секции монтировалось всегда** (требование «прятать `hidden`, а не размонтировать» без оговорки «после первого раскрытия» превратило ленивую загрузку в обязательную — доска рендерилась на каждой карточке сделки) и мёртвый проп `locked`, написанный под несостоявшуюся ветку развилки. **Развилка delivery-доски — противоречие в моём спринт-файле**: задача 2 подразумевала секцию и для внедрения, «Зачем» и задача 4 — обратное; решено по устройству кода, не по числу упоминаний. Смоук гейтом в Chrome: три типа проекта, деплинк, ленивый монтаж на сделке с задачами |
| #82 | docs | вход S-DEAL-ORG-1 сверен с `892e68c` и файл спринта переписан: вход НЕ устарел (LAYOUT-1 влит, `DealOrgBlock` на месте, номера строк `QuotesTab:64/165` совпали) — впервые за эпик. Разведка вынесена в файл готовыми фактами, чтобы CC её не переоткрывал: `QUOTE_STATUS_CONFIG`/`QUOTE_STATUS_TRANSITIONS` живут в `validators/quote.ts`, а не в `lib/constants/` (грепы старой разведки не нашли бы их); `QUOTE_STATUS_TRANSITIONS` заведён в S-QUOTE-1 и **потребителей не имеет** — кнопки решения строятся из него; триггеров, пишущих `projects.budget`, на `quotes` нет (проверено `pg_trigger`) — двухшаговый accept-flow остаётся; партиал-уникальность `quotes_one_accepted_per_project` требует гасить «Принято», а не ловить `23505`. **Дефект найден разведкой:** `DealOrgBlock.tsx:47` печатает `КП v{quotesCount}` — это число КП, а не версия, врёт на любом КП кроме свежего. Две развилки закрыты владельцем: Р-1 причина отклонения → **миграция 131 `quotes.rejection_reason`** (`notes` — текст предложения, а не журнал решений: приписка туда не даёт среза «отклонённые из-за цены»), Р-2 ожидаемый файл → отдельный список «Ждём» из задач в колонках `category='paused'` (`lane='wait'`), иконки файла нет. Номер 131 взят из живой `schema_migrations` (последняя применённая `20260907073548`), не из папки. Правок кода нет |
| #81 | docs | закрытие S-DEAL-LAYOUT-1: журнал и learnings за 07.09 (ленивый монтаж, противоречие в спринт-доке, критерий смоука целит в признак ИМЕННО этого изменения), очередь эпика освобождена полностью, долг кольца снят как выполненный |
| #83 | S-DEAL-ORG-1 | **Виджет W4, организационный блок. Миграция 131 применена гейтом 2026-09-08 (`20260908061745`), advisors без новых WARN.** Карточка активного КП (`accepted` → последнее `sent` → последнее любое) с решением в один клик; кнопки строятся из `QUOTE_STATUS_TRANSITIONS`, у которой с S-QUOTE-1 не было ни одного потребителя. Версия КП вычисляется (`quoteVersionMap`, позиция по `created_at asc`) — до спринта свёрнутая строка печатала `КП v{quotes.length}`, то есть ЧИСЛО КП: на трёх КП второе подписывалось «v3». Срок действия через `diffDaysKey`/`mskDateKey`, `now` аргументом, порог 3 дня одним местом. **Три отклонения от спеки, все осознанные:** «Принято» не двигает стадию (открывает штатную `StageTransitionModal` — прямой `update({stage_id})` завёл бы второй путь мимо гейта 027/078 и мимо `stage_transitions`), бюджет остаётся за двухшаговым accept-flow `QuotesTab`, «ожидаемый файл» стал отдельным списком «Ждём» из задач в колонках `paused` (признака `expected` у `project_files` нет, а задача с иконкой файла врала бы). Раскладка на `@container` — первый контейнерный запрос в проекте: зона «Работа» делит строку с рельсой 356px, и viewport-брейкпоинт мерил бы не то. **Гейт:** гранты проверены отдельно от политик (`role_table_grants` vs `column_privileges`) — при колоночных грантах новая колонка отдавала бы 42501 при исправных RLS. **Cold review 80/100 FIX:** F3 — `stampedAt` печатал `sent_at` под подписью «Отклонено», поднята до блокера и исправлена до мержа (у rejected/draft/expired даты нет вовсе; `rejected_at` не заводили — потребовал бы правки `SECURITY DEFINER`-триггера ради подписи); F1 → S-DEAL-ORG-2. Разведка данных после apply: КП в проде **три**, сделок с >1 КП **ноль** — версионная механика покрыта тестами, но живых случаев пока не имеет |
| #84 | chore/types-131 | реген типов после 131 и снятие подпорок: `supabase.gen.ts` **+3 строки** (`rejection_reason` в Row/Insert/Update `quotes`, ни одного удаления), `QuoteRejectionPatch` удалён — `validators/quote.ts` вернулся к blob `6982a15`, то есть побайтово к состоянию до спринта; `rejection_reason` возвращён в `QUOTE_COLS` — без этого причина писалась бы, но не читалась. Правок логики нет |
| #85 | docs | закрытие S-DEAL-ORG-1: журнал и learnings за 08.09 (вывод про констрейнт сделан до взгляда в данные и оказался неверным; тернарник по двум веткам из трёх; `add column` проверяется грантами; четыре процессные мелочи), STATUS ревизия 44, S-DEAL-ORG-2 заведён в очередь с пометкой про закрывающееся окно. Под git заведены три untracked-файла `_analysis/` — cold review, fix F3, fix типов: они писались в основном чекауте, пока работа шла в worktree, и в PR #83/#84 не попали |

Закрытое до августа — `CHANGELOG.md` и roadmap-ревизии; сюда не переносится.
